id = "moza-native-visible-lane"
title = "Moza R5 native visible motion lane"
status = "active"
owner = "hardware"
created = "2026-05-18"

proposal = "docs/proposals/OR-PROP-0001-moza-native-visible-lane.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md"
handoff = "plans/moza-native-visible-lane/handoff.md"

specs = [
  "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md",
  "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md",
]

adrs = [
  "docs/adr/0009-hardware-validation-evidence-state-machine.md",
]

objective = """
Move the Moza R5 lane from native_response_ready to native_visible_ready using receipt-backed, exact-command hardware attempts while keeping PIDFF diagnosis, passive sniffing, Pit House, SimHub, simulator telemetry, and simulator FFB claim boundaries explicit.
"""

end_state = [
  "A same-lane native visible-motion receipt passes the native_actuator_visible_smoke gate.",
  "The lane manifest, verifier receipt, and audit record native_visible_ready without smoke-ready, release-ready, high-torque, simulator, or vendor-app claims.",
  "Further controlled movement, external compatibility, simulator telemetry, and bounded simulator FFB remain separately gated work.",
]

claim_boundaries = [
  "This active goal does not authorize hardware output.",
  "No direct report 0x20, high torque, serial config, firmware, or DFU is in scope.",
  "Pit House, SimHub, simulator evidence, and passive USB sniff artifacts are not native-control prerequisites.",
  "Dry-runs, lifecycle traces, plans, indexes, and bench-wizard receipts do not satisfy native-visible readiness.",
]

status_docs = [
  "docs/NOW_NEXT_LATER.md",
  "ci/hardware/moza-r5/2026-05-13/index.md",
  "docs/hardware/moza-r5-validation.md",
  "docs/hardware/moza-r5-artifact-checklist.md",
  "docs/hardware/moza-r5-completion-audit.md",
  "docs/hardware/moza-validation-matrix.md",
  "docs/hardware/moza-r5-controlled-angle-analysis.md",
  "docs/hardware/moza-r5-pidff-lifecycle-trace.md",
  "docs/hardware/moza-r5-pidff-effect-lifecycle-plan.md",
  "docs/hardware/moza-r5-standard-pidff-path-diagnosis.md",
  "docs/hardware/moza-r5-post-authority-pidff-response.md",
  "docs/hardware/moza-r5-vendor-control-investigation.md",
  "plans/moza-native-visible-lane/handoff.md",
]

[[work_item]]
id = "activate-source-of-truth"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-activate-source-of-truth"
current_pointer = "ci/hardware/moza-r5/2026-05-13/index.md"
claim_boundary = "Docs and source-of-truth metadata only; no hardware output, no authorization receipt, and no readiness promotion."
commands = [
  "python scripts/policy_file.py",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "git diff --check",
]

[[work_item]]
id = "record-completion-audit"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-record-completion-audit"
current_pointer = "docs/hardware/moza-r5-completion-audit.md"
claim_boundary = "Docs-only audit; no hardware output, no authorization receipt, no readiness promotion, and no new hardware artifact claim."
commands = [
  "python scripts/policy_file.py",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-bench-wizard-current.json --md-out target/moza-bench-wizard-current.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-native-visible-current.json --json",
  "git diff --check",
]

[[work_item]]
id = "document-input-role-candidates"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-document-input-role-candidates"
current_pointer = "ci/hardware/moza-r5/2026-05-13/pre-output-readiness.json"
claim_boundary = "Candidate-only input slot navigation; no hardware output, no authorization receipt, no readiness promotion, and no role-specific semantic claim."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl input_role -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl moza -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "target-only-r5-control-parser-regression"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-target-only-r5-control-parser-regression"
current_pointer = "crates/moza-wheelbase-report/src/lib.rs"
claim_boundary = "Parser regression only; target-only wheel control samples preserve packed R5 V1 button bytes without creating button-name, clutch, rotary, native-visible, smoke-ready, authorization, or output claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p racing-wheel-moza-wheelbase-report parse_wheelbase_input_preserves_target_only_live_r5_v1_button_samples -- --nocapture",
  "cargo clippy --locked -p racing-wheel-moza-wheelbase-report --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "harden-artifact-claim-boundaries"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-harden-artifact-claim-boundaries"
current_pointer = "crates/cli/src/commands/moza.rs"
claim_boundary = "Regression coverage only; valid failed verifier receipts remain diagnostic artifacts and do not become native-visible, smoke-ready, authorization, or output claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl artifact_index -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl moza -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "git diff --check",
]

[[work_item]]
id = "surface-attempt-03-clearance-phrase"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-surface-attempt-03-clearance-phrase"
current_pointer = "crates/cli/src/commands/moza.rs"
claim_boundary = "Operator-packet UX only; bench-wizard remains read-only and does not create authorization receipts, output commands, or readiness claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl moza -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "git diff --check",
]

[[work_item]]
id = "harden-attempt-03-authorization-handoff"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-harden-attempt-03-authorization-handoff"
current_pointer = "crates/cli/src/commands/moza.rs"
claim_boundary = "Bench-wizard navigation only; a recorded attempt-03 authorization is surfaced as a separate handoff state without emitting output commands, creating authorization receipts, or claiming native-visible readiness."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl controlled_angle -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl moza -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "git diff --check",
]

[[work_item]]
id = "surface-attempt-03-planned-artifacts"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-surface-attempt-03-planned-artifacts"
current_pointer = "ci/hardware/moza-r5/2026-05-13/index.md"
claim_boundary = "Artifact-index navigation only; missing attempt-03 authorization/output receipts are surfaced as planned_missing without creating authorization receipts, output receipts, or native-visible claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl artifact_index -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl moza -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "refresh-input-semantic-candidate-artifacts"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-refresh-input-semantic-candidate-artifacts"
current_pointer = "ci/hardware/moza-r5/2026-05-13/lane-capture-analysis.json"
claim_boundary = "No-output artifact refresh only; candidate R5 V1 extended slots are recorded as diagnostic input-topology evidence without creating role-specific semantic claims, authorization receipts, output receipts, or native-visible claims."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- moza analyze-lane --lane ci/hardware/moza-r5/2026-05-13 --json-out ci/hardware/moza-r5/2026-05-13/lane-capture-analysis.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza sync-role-status --lane ci/hardware/moza-r5/2026-05-13 --json-out ci/hardware/moza-r5/2026-05-13/role-status-sync.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-artifact-index-input-semantics.json --md-out ci/hardware/moza-r5/2026-05-13/index.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza sync-role-status --lane ci/hardware/moza-r5/2026-05-13 --check --json-out target/moza-role-status-check.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage passive --json-out target/moza-passive-after-input-semantics-refresh.json --json",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "surface-pit-house-compatibility-progress"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-surface-pit-house-compatibility-progress"
current_pointer = "crates/cli/src/commands/moza.rs"
claim_boundary = "No-output navigation only; Pit House availability and partial case progress are external-smoke diagnostics and do not become native-control, native-visible, smoke-ready, authorization, or output claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl artifact_navigation_surfaces_pit_house -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl artifact_index -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "git diff --check",
]

[[work_item]]
id = "surface-simulator-compatibility-progress"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-surface-simulator-compatibility-progress"
current_pointer = "crates/cli/src/commands/moza.rs"
claim_boundary = "No-output navigation only; simulator telemetry and bounded simulator FFB progress are external-smoke diagnostics and do not become native-control, native-visible, smoke-ready, authorization, or output claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl artifact_navigation_surfaces_simulator -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl simulator_navigation -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl artifact_index -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "git diff --check",
]

[[work_item]]
id = "surface-passive-sniff-navigation"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-surface-passive-sniff-navigation"
current_pointer = "crates/cli/src/commands/moza.rs"
claim_boundary = "No-output navigation only; passive USB sniff scenarios are protocol research/support diagnostics and do not become native-control, native-visible, smoke-ready, authorization, or output claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl passive_sniff -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl artifact_index -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "attempt-03-authorization"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-attempt-03-authorization"
current_pointer = "ci/hardware/moza-r5/2026-05-13/native-controlled-angle-attempt-03-authorization.json"
claim_boundary = "Consumed exact attempt-03 authorization receipt; no motion claim and no further output permission."
blocked_by = "n/a"
required_bench_clear_evidence = "bench clear for exactly one Moza controlled-angle attempt 03: target 1 degree, max 5%, timeout 2000 ms, strategy pidff-bounded-effect, profile bounded-pidff-effect-lifecycle-v1, R5 stable, KS attached securely, hands clear, wheel clear, prior undertravel receipts preserved"
commands = [
  "wheelctl moza pre-output-readiness --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-pre-output-before-attempt-03.json --json",
  "wheelctl moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-native-visible-before-attempt-03.json --json",
]

[[work_item]]
id = "attempt-03-output"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-attempt-03-output"
current_pointer = "ci/hardware/moza-r5/2026-05-13/native-controlled-angle-attempt-03-smoke.json"
claim_boundary = "Exactly one authorized 1 degree, 5 percent, 2000 ms controlled-angle attempt ran and failed safely; no rerun or escalation."
blocked_by = "n/a"
commands = [
  "wheelctl moza controlled-angle-smoke --device hid-0x346E-0x0004-if2-0x0001-0x0004 --lane ci/hardware/moza-r5/2026-05-13 --prior-actuator-proof ci/hardware/moza-r5/2026-05-13/native-actuator-profile-smoke.json --steering-proof ci/hardware/moza-r5/2026-05-13/steering-angle-stream-proof.json --authorization-proof ci/hardware/moza-r5/2026-05-13/native-controlled-angle-attempt-03-authorization.json --target-degrees 1 --profile bounded-pidff-effect-lifecycle-v1 --max-percent 5 --timeout-ms 2000 --strategy pidff-bounded-effect --confirm-controlled-angle --json-out ci/hardware/moza-r5/2026-05-13/native-controlled-angle-attempt-03-smoke.json --json",
]

[[work_item]]
id = "attempt-03-analysis"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-attempt-03-analysis"
current_pointer = "ci/hardware/moza-r5/2026-05-13/native-controlled-angle-attempt-03-failure-analysis.json"
claim_boundary = "No-output analysis only; preserves the consumed authorization and failed output receipt without creating rerun, force escalation, native-visible, smoke-ready, or release-ready claims."
blocked_by = "n/a"
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out ci/hardware/moza-r5/2026-05-13/native-visible-verification.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-artifact-index-after-attempt-03.json --md-out ci/hardware/moza-r5/2026-05-13/index.md --json",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "standard-pidff-path-diagnosis"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-standard-pidff-path-diagnosis"
current_pointer = "ci/hardware/moza-r5/2026-05-13/native-pidff-standard-path-diagnosis.json"
claim_boundary = "No-output protocol architecture diagnosis only; preserves all three undertravel receipts without creating authorization, output, native-visible, smoke-ready, or release-ready claims."
blocked_by = "n/a"
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-native-visible-after-standard-pidff-diagnosis.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-artifact-index-after-standard-pidff-diagnosis.json --md-out ci/hardware/moza-r5/2026-05-13/index.md --json",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "vendor-control-sniff-plans"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-vendor-control-sniff-plans"
current_pointer = "ci/hardware/sniff/moza-r5/2026-05-13"
claim_boundary = "No-output passive sniff planning only; creates non-claiming plan artifacts without pcap receipts, summaries, authorization, output, native-visible, smoke-ready, or release-ready claims."
blocked_by = "n/a"
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-plan --family moza-r5 --scenario pit-house-open-idle --lane ci/hardware/moza-r5/2026-05-13 --operator Steven --device-note <device-note> --capture-tool usbpcap --capture-tool wireshark --capture-tool tshark --platform-hint windows --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-plan.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-plan --family moza-r5 --scenario pit-house-setting-change --lane ci/hardware/moza-r5/2026-05-13 --operator Steven --device-note <device-note> --capture-tool usbpcap --capture-tool wireshark --capture-tool tshark --platform-hint windows --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/sniff-plan.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-plan --family moza-r5 --scenario simhub-open-idle --lane ci/hardware/moza-r5/2026-05-13 --operator Steven --device-note <device-note> --capture-tool usbpcap --capture-tool wireshark --capture-tool tshark --platform-hint windows --json-out ci/hardware/sniff/moza-r5/2026-05-13/simhub-open-idle/sniff-plan.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-plan --family moza-r5 --scenario simhub-output-session --lane ci/hardware/moza-r5/2026-05-13 --operator Steven --device-note <device-note> --capture-tool usbpcap --capture-tool wireshark --capture-tool tshark --platform-hint windows --json-out ci/hardware/sniff/moza-r5/2026-05-13/simhub-output-session/sniff-plan.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-plan --family moza-r5 --scenario simulator-session-start-stop --lane ci/hardware/moza-r5/2026-05-13 --operator Steven --device-note <device-note> --capture-tool usbpcap --capture-tool wireshark --capture-tool tshark --platform-hint windows --json-out ci/hardware/sniff/moza-r5/2026-05-13/simulator-session-start-stop/sniff-plan.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-artifact-index-after-vendor-sniff-plans.json --md-out ci/hardware/moza-r5/2026-05-13/index.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-native-visible-after-vendor-sniff-plans.json --json",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "bench-wizard-vendor-sniff-next-step"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-bench-wizard-vendor-sniff-next-step"
current_pointer = "wheelctl moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13"
claim_boundary = "No-output operator navigation only; surfaces command-bound passive sniff receipt and summary guidance from bench-wizard and verifier output without opening HID, creating authorization, emitting output, or making readiness claims."
blocked_by = "n/a"
commands = [
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-bench-wizard-after-sniff-next.json --md-out target/moza-bench-wizard-after-sniff-next.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-native-visible-after-bench-wizard-sniff-next.json --json",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "bench-wizard-sniff-command-validation"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-bench-wizard-sniff-command-validation"
current_pointer = "wheelctl moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13"
claim_boundary = "No-output test guard only; parse-validates generated passive sniff receipt, summary, and bundle handoff commands without opening HID, creating authorization, emitting output, or making readiness claims."
blocked_by = "n/a"
commands = [
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard_sniff_next_operator_commands_parse -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-bench-wizard-after-sniff-command-validation.json --md-out target/moza-bench-wizard-after-sniff-command-validation.md --json",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "bench-wizard-sniff-capture-checklist"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-bench-wizard-sniff-capture-checklist"
current_pointer = "wheelctl moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13"
claim_boundary = "No-output operator checklist only; describes external USBPcap/Wireshark/tshark capture steps for the next passive sniff without opening HID, creating authorization, emitting output, creating pcap receipts, or making readiness claims."
blocked_by = "n/a"
commands = [
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard_points_diagnosed_attempt_03_to_passive_sniff_capture -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-bench-wizard-after-sniff-capture-checklist.json --md-out target/moza-bench-wizard-after-sniff-capture-checklist.md --json",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "passive-sniff-bundle-json-out"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-passive-sniff-bundle-json-out"
current_pointer = "crates/cli/src/commands/hardware.rs"
claim_boundary = "No-output artifact packaging only; adds a separate sniff bundle manifest JSON writer without opening HID, creating authorization, emitting output, or making readiness claims."
blocked_by = "n/a"
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_bundle -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl passive_sniff -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl parse_hardware_sniff_bundle -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "closed-loop-native-motion-ladder"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-closed-loop-native-motion-ladder"
current_pointer = "ci/hardware/moza-r5/2026-05-13/native-controlled-angle-closed-loop-smoke.json"
claim_boundary = "Exactly one authorized 1 degree, 5 percent, 2000 ms closed-loop controlled-angle attempt ran and failed safely; no rerun, force escalation, native-visible, smoke-ready, or release-ready claim."
blocked_by = "n/a"
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl controlled_angle -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl native_visible -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "passive-sniff-report-classification"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-passive-sniff-report-classification"
current_pointer = "crates/cli/src/commands/hardware.rs"
claim_boundary = "No-output protocol navigation only; sniff-summary classifies passive report IDs as standard PIDFF, vendor/device-specific candidates, or input/status without creating native-control, native-visible, smoke-ready, authorization, or output claims."
blocked_by = "n/a"
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl hardware_sniff_summary -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_summary -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "pit-house-open-idle-sniff-evidence"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-pit-house-open-idle-sniff-evidence"
current_pointer = "ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle"
claim_boundary = "No-output passive evidence only; records a Pit House open-idle USB sniff receipt, classified summary, and bundle manifest without committing raw pcap, opening HID, emitting output, or making native-control, native-visible, smoke-ready, or release-ready claims."
blocked_by = "n/a"
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-receipt --plan ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-plan.json --pcapng target/sniff/pit-house-open-idle/capture.pcapng --operator Steven --app \"MOZA Pit House\" --scenario pit-house-open-idle --evidence <operator-evidence> --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-receipt.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-summary --pcapng target/sniff/pit-house-open-idle/capture.pcapng --vendor 0x346E --product 0x0004 --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-summary.json --md-out target/sniff/pit-house-open-idle/sniff-summary.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-bundle --plan ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-plan.json --receipt ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-receipt.json --summary ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-summary.json --operator-notes target/sniff/pit-house-open-idle/operator-notes.md --out target/sniff/pit-house-open-idle/openracing-sniff-bundle.zip --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-bundle-manifest.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-after-pit-house-open-idle.json --md-out ci/hardware/moza-r5/2026-05-13/index.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-current/native-visible-after-pit-house-open-idle.json --json; if ($LASTEXITCODE -ne 4) { throw \"expected native-visible verifier to remain blocked\" }",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "passive-sniff-operator-notes-template"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-passive-sniff-operator-notes-template"
current_pointer = "crates/cli/src/commands/hardware.rs"
claim_boundary = "No-output operator handoff only; sniff plans carry capture checklists, required operator-note fields, and raw-pcap commit default false, while sniff-notes-template writes a non-claiming Markdown notes template without HID opens, hardware output, authorization, native-control, native-visible, smoke-ready, or release-ready claims."
blocked_by = "n/a"
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_plan -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_notes_template -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard_sniff_next_operator_commands_parse -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  ".\\target\\debug\\wheelctl.exe moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-native-visible-after-sniff-notes-template.json --json; if ($LASTEXITCODE -eq 4) { exit 0 } else { throw \"expected native-visible verifier to remain blocked\" }",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "pit-house-install-source-guidance"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-pit-house-install-source-guidance"
current_pointer = "crates/cli/src/commands/moza.rs"
claim_boundary = "No-output operator navigation only; Pit House availability receipts and navigation surfaces point to the official MOZA Pit House Downloads source without installing, launching, capturing, opening HID, authorizing output, or making native-control, native-visible, smoke-ready, coexistence, firmware, or semantic-control claims."
blocked_by = "n/a"
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl pit_house -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl artifact_index -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  ".\\target\\debug\\wheelctl.exe moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-native-visible-after-pit-house-install-guidance.json --json; if ($LASTEXITCODE -eq 4) { exit 0 } else { throw \"expected native-visible verifier to remain blocked\" }",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "pit-house-open-standard-case-evidence"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-pit-house-open-standard-case-evidence"
current_pointer = "ci/hardware/moza-r5/2026-05-13/pit-house-open-standard.json"
claim_boundary = "No-output external compatibility case evidence only; records Pit House open-idle standard process/window evidence, observation receipt, and case artifact linked to init-standard.json without opening HID or serial, sending output/configuration/firmware writes, creating authorization, running vendor-authority attempts, claiming Pit House parent coexistence, native-control, native-visible, smoke-ready, simulator, firmware, or release-ready."
blocked_by = "n/a"
commands = [
  "wheelctl moza pit-house-evidence --case open-standard --operator Steven --evidence \"Pit House open and idle while standard mode completed.\" --require-match --json-out ci/hardware/moza-r5/2026-05-13/pit-house-evidence-open-standard.json --overwrite --json",
  "wheelctl moza pit-house-observation --case open-standard --evidence-kind process-window-snapshot --evidence-artifact pit-house-evidence-open-standard.json --operator Steven --evidence \"Pit House open and idle while standard mode completed.\" --json-out ci/hardware/moza-r5/2026-05-13/pit-house-observation-open-standard.json --overwrite --json",
  "wheelctl moza pit-house-case --lane ci/hardware/moza-r5/2026-05-13 --case open-standard --observation-artifact pit-house-observation-open-standard.json --evidence \"Pit House open and idle; standard mode completed without conflict.\" --json-out ci/hardware/moza-r5/2026-05-13/pit-house-open-standard.json --overwrite --json",
  "wheelctl moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-after-pit-house-open-standard.json --md-out ci/hardware/moza-r5/2026-05-13/index.md --json",
  "wheelctl moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/bench-wizard-after-pit-house-open-standard.json --md-out target/moza-current/bench-wizard-after-pit-house-open-standard.md --json",
  "wheelctl moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-current/native-visible-after-pit-house-open-standard.json --json; if ($LASTEXITCODE -eq 4) { exit 0 } else { throw \"expected native-visible verifier to remain blocked\" }",
  "cargo test --locked -p wheelctl --bin wheelctl pit_house -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl artifact_index -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard -- --nocapture",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "brake-hbp-semantic-promotion"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-brake-hbp-semantic-promotion"
current_pointer = "crates/moza-wheelbase-report/src/lib.rs"
claim_boundary = "Parser and no-output artifact refresh only; promotes checked-in isolated R5 V1 through-hub brake and HBP handbrake samples to semantic axes while leaving clutch generic and making no wheel-button, native-control, native-visible, smoke-ready, Pit House coexistence, hardware-output, authorization, HID-open, or new-capture claims."
blocked_by = "n/a"
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p racing-wheel-moza-wheelbase-report --all-features -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl input_role -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl artifact_index -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza validate-captures --lane ci/hardware/moza-r5/2026-05-13 --json-out ci/hardware/moza-r5/2026-05-13/parser-fixture-validation.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza promote-fixtures --lane ci/hardware/moza-r5/2026-05-13 --fixture-dir crates/hid-moza-protocol/fixtures/moza-r5-2026-05-13 --json-out ci/hardware/moza-r5/2026-05-13/fixture-promotion.json --overwrite --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza analyze-lane --lane ci/hardware/moza-r5/2026-05-13 --json-out ci/hardware/moza-r5/2026-05-13/lane-capture-analysis.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza sync-role-status --lane ci/hardware/moza-r5/2026-05-13 --json-out ci/hardware/moza-r5/2026-05-13/role-status-sync.json --json",
  ".\\target\\debug\\wheelctl.exe moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out ci/hardware/moza-r5/2026-05-13/native-visible-verification.json --json; if ($LASTEXITCODE -eq 4) { exit 0 } else { throw \"expected native-visible verifier to remain blocked\" }",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-artifact-index-after-brake-hbp-semantics.json --md-out ci/hardware/moza-r5/2026-05-13/index.md --json",
  ".\\target\\debug\\wheelctl.exe moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-native-visible-after-brake-hbp-semantics.json --json; if ($LASTEXITCODE -eq 4) { exit 0 } else { throw \"expected native-visible verifier to remain blocked\" }",
  "cargo clippy --locked -p racing-wheel-moza-wheelbase-report --all-features -- -D warnings",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "pit-house-full-controls-sniff-evidence"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-pit-house-full-controls-sniff-evidence"
current_pointer = "ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls"
claim_boundary = "No-output passive evidence only; records a Pit House full-controls USB sniff plan, receipt, classified summary, and bundle manifest without committing raw pcap, opening HID, emitting output, or making native-control, native-visible, smoke-ready, coexistence, simulator, firmware, semantic-control, or release-ready claims."
blocked_by = "n/a"
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-plan --family moza-r5 --scenario pit-house-full-controls --lane ci/hardware/moza-r5/2026-05-13 --operator Steven --device-note <device-note> --capture-tool usbpcap --capture-tool wireshark --capture-tool tshark --platform-hint windows --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-plan.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-receipt --plan ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-plan.json --pcapng target/sniff/pit-house-full-controls/capture.pcapng --operator Steven --app \"MOZA Pit House 1.3.8.38 release\" --scenario pit-house-full-controls --evidence <operator-evidence> --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-receipt.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-summary --pcapng target/sniff/pit-house-full-controls/capture.pcapng --vendor 0x346E --product 0x0004 --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-summary.json --md-out target/sniff/pit-house-full-controls/sniff-summary.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-bundle --plan ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-plan.json --receipt ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-receipt.json --summary ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-summary.json --operator-notes target/sniff/pit-house-full-controls/operator-notes.md --out target/sniff/pit-house-full-controls/openracing-sniff-bundle.zip --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-bundle-manifest.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-after-pit-house-full-controls.json --md-out ci/hardware/moza-r5/2026-05-13/index.md --json",
  ".\\target\\debug\\wheelctl.exe moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-current/native-visible-after-pit-house-full-controls.json --json; if ($LASTEXITCODE -eq 4) { exit 0 } else { throw \"expected native-visible verifier to remain blocked\" }",
  "cargo test --locked -p wheelctl --bin wheelctl passive_sniff -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl artifact_index -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "moza-current-state-status-refresh"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-moza-current-state-status-refresh"
current_pointer = "docs/hardware/moza-r5-completion-audit.md"
claim_boundary = "Docs and source-of-truth status refresh only; aligns handoff and audit text to current checked-in lane receipts without opening HID, creating captures, authorizing output, or making native-control, native-visible, smoke-ready, coexistence, simulator, firmware, or release-ready claims."
blocked_by = "n/a"
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-after-status-refresh.json --md-out target/moza-current/artifact-index-after-status-refresh.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/bench-wizard-after-status-refresh.json --md-out target/moza-current/bench-wizard-after-status-refresh.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-current/native-visible-after-status-refresh.json --json; if ($LASTEXITCODE -eq 4) { exit 0 } else { throw \"expected native-visible verifier to remain blocked\" }",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "refresh-pre-output-readiness-receipt"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-refresh-pre-output-readiness-receipt"
current_pointer = "ci/hardware/moza-r5/2026-05-13/pre-output-readiness.json"
claim_boundary = "No-output generated receipt refresh only; updates pre-output readiness navigation from current checked-in lane receipts without opening HID, creating captures, authorizing output, or making native-control, native-visible, smoke-ready, coexistence, simulator, firmware, or release-ready claims."
blocked_by = "n/a"
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- moza pre-output-readiness --lane ci/hardware/moza-r5/2026-05-13 --json-out ci/hardware/moza-r5/2026-05-13/pre-output-readiness.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-current/native-visible-after-pre-output-refresh.json --json; if ($LASTEXITCODE -eq 4) { exit 0 } else { throw \"expected native-visible verifier to remain blocked\" }",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "vendor-command-registry-risk-policy"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "docs/hardware/moza-r5-vendor-authority-test-plan.md#pr-sequence"
current_pointer = "fixtures/moza/r5/vendor-command-registry.json"
claim_boundary = "No-output semantic registry only; completes required vendor command families and risk policy tests without codec framing, transport, read-only hardware probes, authorization receipts, hardware writes, native-visible, smoke-ready, coexistence, simulator, firmware, or release-ready claims."
blocked_by = "n/a"
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_authority_registry -- --nocapture",
  "cargo clippy --locked -p racing-wheel-hid-moza-protocol --all-targets --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "vendor-serial-codec-fixtures"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "docs/hardware/moza-r5-vendor-authority-test-plan.md#pr-sequence"
current_pointer = "fixtures/moza/r5/vendor-serial-codec-fixtures.json"
claim_boundary = "No-output fixture decode only; validates checked-in synthetic serial frame bytes against the vendor command registry without encode APIs, fake transport, CLI, read-only hardware probes, authorization receipts, serial device open, query sends, output writes, configuration writes, hardware writes, native-visible, smoke-ready, coexistence, simulator, firmware, or release-ready claims."
blocked_by = "n/a"
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_serial_codec_fixtures -- --nocapture",
  "cargo clippy --locked -p racing-wheel-hid-moza-protocol --all-targets --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "vendor-fake-serial-transport"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "docs/hardware/moza-r5-vendor-authority-test-plan.md#pr-sequence"
current_pointer = "fixtures/moza/r5/vendor-fake-serial-transport.json"
claim_boundary = "No-output fake transport only; replays checked-in synthetic serial fixture bytes through the decoder, accepts only read-only status fixtures, and rejects write-like candidates without CLI, serial device open, query sends, read-only hardware probes, authorization receipts, output writes, configuration writes, hardware writes, native-visible, smoke-ready, coexistence, simulator, firmware, or release-ready claims."
blocked_by = "n/a"
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_fake_serial_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_serial_codec_fixtures -- --nocapture",
  "cargo clippy --locked -p racing-wheel-hid-moza-protocol --all-targets --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "vendor-no-output-cli-tools"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "docs/hardware/moza-r5-vendor-authority-test-plan.md#pr-sequence"
current_pointer = "crates/cli/src/commands/moza.rs"
claim_boundary = "No-output CLI tooling only; adds wheelctl moza vendor-fake-transport to exercise checked-in synthetic serial fixture bytes through the fake transport and write a non-claiming receipt without HID opens, serial device opens, read-only hardware probes, query sends, authorization receipts, output writes, configuration writes, hardware writes, native-visible, smoke-ready, coexistence, simulator, firmware, or release-ready claims."
blocked_by = "n/a"
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl parse_moza_vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_fake_serial_transport -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza vendor-fake-transport --json-out target/moza-current/vendor-no-output-cli.json --json",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "vendor-read-only-status-probe"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "docs/hardware/moza-r5-vendor-authority-test-plan.md#pr-sequence"
current_pointer = "crates/cli/src/commands/moza.rs"
claim_boundary = "Read-only serial status probe tooling only; adds wheelctl moza vendor-status-probe with explicit confirmation and MOZA R5 USB serial identity gating, sends only registry-allowed read-only status queries, and writes a non-claiming receipt without HID opens, output writes, configuration writes, firmware/DFU commands, authorization receipts, native-control, native-visible, smoke-ready, coexistence, simulator, or release-ready claims."
blocked_by = "n/a"
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_status_probe -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_authority_registry -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl parse_moza_vendor_status_probe -- --nocapture",
  "cargo clippy --locked -p racing-wheel-hid-moza-protocol --all-targets --all-features -- -D warnings",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo hakari verify",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "vendor-exact-authorization-support"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "docs/hardware/moza-r5-vendor-authority-test-plan.md#pr-sequence"
current_pointer = "schemas/moza-vendor-authority-authorization.schema.json"
claim_boundary = "Exact authorization receipt support only; adds wheelctl moza authorize-vendor-authority to create a single-use command/frame/hash-bound authorization receipt without opening HID, opening serial, sending queries, sending output/configuration/firmware writes, consuming authorization, native-control, native-visible, smoke-ready, coexistence, simulator, or release-ready claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_authority_authorization -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl parse_moza_authorize_vendor_authority -- --nocapture",
  "cargo test --locked -p wheelctl --test cli_comprehensive_e2e_tests help_snapshots::snapshot_moza_help -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo hakari verify",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "vendor-authority-smoke-dry-run"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "docs/hardware/moza-r5-vendor-authority-test-plan.md#pr-sequence"
current_pointer = "schemas/moza-vendor-authority-smoke.schema.json"
claim_boundary = "No-output vendor-authority smoke dry-run only; adds wheelctl moza vendor-authority-smoke-dry-run to validate an exact authorization receipt and emit a non-claiming dry-run receipt without opening HID, opening serial, sending queries, sending output/configuration/firmware writes, consuming authorization, native-control, native-visible, smoke-ready, coexistence, simulator, or release-ready claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_authority_smoke -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl parse_moza_vendor_authority_smoke_dry_run -- --nocapture",
  "cargo test --locked -p wheelctl --test cli_comprehensive_e2e_tests help_snapshots::snapshot_moza_help -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo hakari verify",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "vendor-authority-attempt-contract"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "docs/hardware/moza-r5-vendor-authority-test-plan.md#pr-sequence"
current_pointer = "schemas/moza-vendor-authority-attempt.schema.json"
claim_boundary = "First bounded vendor-authority attempt contract only; defines the consumed, non-claiming hardware-attempt receipt shape without adding a CLI command, opening HID, opening serial, sending read-only queries, sending output/configuration/firmware writes, native-control, native-visible, smoke-ready, coexistence, simulator, or release-ready claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_authority_attempt_schema -- --nocapture",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "vendor-authority-attempt-cli"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "docs/hardware/moza-r5-vendor-authority-test-plan.md#pr-sequence"
current_pointer = "schemas/moza-vendor-authority-attempt.schema.json"
claim_boundary = "Guarded first bounded vendor-authority attempt CLI only; adds a command path that can consume exact authorization and send exactly one bound serial frame when explicitly run, but this PR does not run hardware output, check in hardware attempt evidence, native-control, native-visible, smoke-ready, coexistence, simulator, or release-ready claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_authority_attempt -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl parse_moza_vendor_authority_attempt -- --nocapture",
  "cargo test --locked -p wheelctl --test cli_comprehensive_e2e_tests help_snapshots::snapshot_moza_help -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo hakari verify",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "vendor-authority-attempt-handoff"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "docs/hardware/moza-r5-vendor-authority-test-plan.md#pr-sequence"
current_pointer = "crates/cli/src/commands/moza.rs"
claim_boundary = "No-output operator handoff only; artifact-index and bench-wizard surface the exact vendor-authority command id, frame, required bench-clear text, authorization receipt, and smoke dry-run paths after closed-loop PIDFF undertravel, but they do not create authorization, emit the hardware attempt command, run serial writes, native-control, native-visible, smoke-ready, coexistence, simulator, or release-ready claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_authority_handoff -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl checked_in_moza_lane_index_matches_artifact_index_renderer -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo hakari verify",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "vendor-post-authority-pidff-response"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "docs/hardware/moza-r5-vendor-authority-test-plan.md#pr-sequence"
current_pointer = "schemas/moza-vendor-post-authority-pidff-response.schema.json"
claim_boundary = "No-output post-authority PIDFF response comparison only; adds a command and navigation path to compare a consumed vendor-authority attempt receipt against baseline and post-authority PIDFF response receipts without opening HID, opening serial, sending queries, sending PIDFF or serial output, creating authorization, reusing authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, or release-ready claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_post_authority_pidff_response -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl post_authority_comparison -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl parse_moza_vendor_post_authority_pidff_response -- --nocapture",
  "cargo test --locked -p wheelctl --test cli_comprehensive_e2e_tests help_snapshots::snapshot_moza_help -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo hakari verify",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "vendor-authority-attempt-blocked-receipt"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "docs/hardware/moza-r5-vendor-authority-test-plan.md#pr-sequence"
current_pointer = "schemas/moza-vendor-authority-attempt-blocked.schema.json"
claim_boundary = "Blocked-before-send vendor-authority attempt receipt only; if the guarded attempt validates exact receipts and R5 serial identity but fails before opening serial or sending the frame, record a non-claiming blocked receipt without consuming authorization, satisfying the consumed attempt gate, sending output/configuration/firmware writes, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, or release-ready claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_authority_attempt_blocked -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_authority_handoff -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "vendor-authority-serial-precondition-handoff"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "docs/hardware/moza-r5-vendor-authority-test-plan.md#pr-sequence"
current_pointer = "crates/cli/src/commands/moza.rs"
claim_boundary = "No-output operator navigation only; artifact-index and bench-wizard surface exclusive R5 serial/CDC access guidance and stored serial-port hints before exact vendor-authority authorization or a separate bounded attempt, without opening live serial, creating authorization, emitting hardware attempt commands, running output/configuration/firmware writes, native-control, native-visible, smoke-ready, Pit House dependency, simulator, or release-ready claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_authority_handoff -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "refresh-pre-output-readiness-after-pit-house-open-standard"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-refresh-pre-output-readiness-after-pit-house-open-standard"
current_pointer = "ci/hardware/moza-r5/2026-05-13/pre-output-readiness.json"
claim_boundary = "No-output generated receipt refresh only; records the checked-in Pit House open-standard case in pre-output readiness without opening HID or serial, creating authorization, running output, proving Pit House coexistence, or promoting native-visible, smoke-ready, simulator, firmware, or release-ready claims."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- moza pre-output-readiness --lane ci/hardware/moza-r5/2026-05-13 --json-out ci/hardware/moza-r5/2026-05-13/pre-output-readiness.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-current/native-visible-after-pit-house-open-standard-pre-output-refresh.json --json; if ($LASTEXITCODE -eq 4) { exit 0 } else { throw \"expected native-visible verifier to remain blocked\" }",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "vendor-authority-explicit-authorization-handoff"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "docs/hardware/moza-r5-vendor-authority-test-plan.md#pr-sequence"
current_pointer = "crates/cli/src/commands/moza.rs"
claim_boundary = "No-output bench-wizard handoff hardening only; the generated exact authorization command names the operator and expiry explicitly while still creating no authorization receipt, opening no HID or serial device, emitting no hardware attempt command, sending no output/configuration/firmware writes, and making no native-control, native-visible, smoke-ready, or release-ready claim."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_authority_handoff -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "vendor-authority-live-hardware-doctor-handoff"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "docs/hardware/moza-r5-vendor-authority-test-plan.md#pr-sequence"
current_pointer = "crates/cli/src/commands/moza.rs"
claim_boundary = "No-output bench-wizard/artifact-index handoff hardening only; surfaces a target-only hardware-doctor refresh command that must be reviewed immediately before exact authorization while still creating no authorization receipt, opening no HID or serial device, emitting no hardware attempt command, sending no query/output/configuration/firmware writes, and making no native-control, native-visible, smoke-ready, or release-ready claim."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_authority_handoff -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "vendor-authority-precondition-bound-authorization"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "docs/hardware/moza-r5-vendor-authority-test-plan.md#pr-sequence"
current_pointer = "schemas/moza-vendor-authority-authorization.schema.json"
claim_boundary = "Exact authorization gate hardening only; require the refreshed target-only hardware-doctor precondition receipt to be fresh, observe-only, R5 serial/CDC present, and vendor-app-free before creating a single-use authorization, while still opening no HID or serial device, sending no query/output/configuration/firmware writes, emitting no hardware attempt command, and making no native-control, native-visible, smoke-ready, or release-ready claim."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_authority_authorization -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_authority_handoff -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl parse_moza_authorize_vendor_authority -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "vendor-authority-consumed-attempt-evidence"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "docs/hardware/moza-r5-vendor-authority-test-plan.md#pr-sequence"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-authority-attempt.json"
claim_boundary = "Consumed exact vendor-authority attempt evidence only; records one bench-authorized estop_set_ffb serial frame after a fresh precondition-bound authorization and no-output smoke dry-run, consumes authorization, closes hardware_output_authorized=false, and makes no native-control, native-visible, smoke-ready, release-ready, PIDFF follow-up, retry, high-torque, firmware, or simulator claim."
commands = [
  "cargo test --locked -p wheelctl --bin wheelctl vendor_authority_attempt -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl checked_in_moza_lane_index_matches_artifact_index_renderer -- --nocapture",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "vendor-post-authority-pidff-smoke-authorization"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "docs/hardware/moza-r5-vendor-authority-test-plan.md#pr-sequence"
current_pointer = "crates/cli/src/commands/moza.rs"
claim_boundary = "Guarded post-authority PIDFF smoke authorization only; permits one exact descriptor-proven PIDFF response capture at vendor-post-authority-pidff-smoke.json after a consumed vendor-authority attempt, without replacing native-actuator-visible-smoke.json, reusing vendor authority, emitting commands from bench-wizard, high torque, direct HID report 0xaf, serial config, firmware, native-control, native-visible, smoke-ready, simulator, coexistence, or release-ready claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl post_authority_pidff_smoke -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_post_authority_pidff_response -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "vendor-post-authority-pidff-response-evidence"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "docs/hardware/moza-r5-vendor-authority-test-plan.md#pr-sequence"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-post-authority-pidff-response.json"
claim_boundary = "Post-authority PIDFF response evidence only; records one separately authorized 5 percent, 2000 ms constant-low-force PIDFF response capture after the consumed vendor-authority attempt plus a no-output comparison receipt, preserving final Stop All cleanup and leaving native-control, native-visible, smoke-ready, release-ready, retry, force-escalation, high-torque, direct HID report 0xaf, serial config, firmware, Pit House, simulator, and coexistence claims false."
commands = [
  "cargo test --locked -p wheelctl --bin wheelctl vendor_post_authority_pidff_response -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl checked_in_moza_lane_index_matches_artifact_index_renderer -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-current/native-visible-after-post-authority-pidff.json --json",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "post-authority-pidff-regression-doc-refresh"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-post-authority-pidff-regression-doc-refresh"
current_pointer = "docs/hardware/moza-r5-post-authority-pidff-response.md"
claim_boundary = "Docs/source-of-truth refresh only; records that the consumed vendor-authority attempt and post-authority PIDFF response comparison regressed versus baseline and leaves native-control, native-visible, smoke-ready, release-ready, retry, force-escalation, high-torque, direct HID report 0xaf, serial config, firmware, Pit House, simulator, and coexistence claims false."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/bench-wizard-after-post-authority-doc-refresh.json --md-out target/moza-current/bench-wizard-after-post-authority-doc-refresh.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-current/native-visible-after-post-authority-doc-refresh.json --json; if ($LASTEXITCODE -eq 4) { exit 0 } else { throw \"expected native-visible verifier to remain blocked\" }",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "vendor-protocol-evidence-review"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-vendor-protocol-evidence-review"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json"
claim_boundary = "No-output protocol evidence review only; reads checked-in passive sniff summaries, the command registry, the consumed vendor-authority attempt, and post-authority PIDFF response receipt without opening HID, opening serial, sending read-only queries, sending output/configuration/firmware writes, creating authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, or release-ready claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_protocol_evidence_review -- --nocapture",
  "cargo test --locked -p wheelctl --test cli_comprehensive_e2e_tests help_snapshots::snapshot_moza_help -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza vendor-protocol-evidence-review --lane ci/hardware/moza-r5/2026-05-13 --json-out ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json --json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-after-vendor-protocol-evidence-review.json --md-out ci/hardware/moza-r5/2026-05-13/index.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/bench-wizard-after-vendor-protocol-evidence-review.json --md-out target/moza-current/bench-wizard-after-vendor-protocol-evidence-review.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-current/native-visible-after-vendor-protocol-evidence-review.json --json; if ($LASTEXITCODE -eq 4) { exit 0 } else { throw \"expected native-visible verifier to remain blocked\" }",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "passive-sniff-decode-gap-review"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-passive-sniff-decode-gap-review"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json"
claim_boundary = "No-output passive sniff decode-gap classification only; derives host-to-device packets that lack extractable report IDs from checked-in summaries and refreshes the protocol evidence review without opening HID, opening serial, sending read-only queries, sending hardware output, creating authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, or release-ready claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_summary_surfaces_host_to_device_decode_gaps -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_protocol_evidence_review -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza vendor-protocol-evidence-review --lane ci/hardware/moza-r5/2026-05-13 --json-out ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json --json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-after-passive-sniff-decode-gap-review.json --md-out ci/hardware/moza-r5/2026-05-13/index.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-current/native-visible-after-passive-sniff-decode-gap-review.json --json; if ($LASTEXITCODE -eq 4) { exit 0 } else { throw \"expected native-visible verifier to remain blocked\" }",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "passive-sniff-payload-export-coverage"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-passive-sniff-payload-export-coverage"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json"
claim_boundary = "No-output passive sniff payload-export coverage only; records host-to-device packets with USB data length but no extracted payload bytes from checked-in Pit House summaries without opening HID, opening serial, sending read-only queries, sending hardware output, creating authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, or release-ready claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_summary_surfaces_host_to_device_decode_gaps -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_protocol_evidence_review -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-summary --pcapng target/sniff/pit-house-open-idle/capture.pcapng --vendor 0x346E --product 0x0004 --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-summary.json --md-out target/sniff/pit-house-open-idle/sniff-summary.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-summary --pcapng target/sniff/pit-house-full-controls/capture.pcapng --vendor 0x346E --product 0x0004 --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-summary.json --md-out target/sniff/pit-house-full-controls/sniff-summary.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-bundle --plan ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-plan.json --receipt ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-receipt.json --summary ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-summary.json --operator-notes target/sniff/pit-house-open-idle/operator-notes.md --out target/sniff/pit-house-open-idle/openracing-sniff-bundle.zip --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-bundle-manifest.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-bundle --plan ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-plan.json --receipt ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-receipt.json --summary ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-summary.json --operator-notes target/sniff/pit-house-full-controls/operator-notes.md --out target/sniff/pit-house-full-controls/openracing-sniff-bundle.zip --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-bundle-manifest.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza vendor-protocol-evidence-review --lane ci/hardware/moza-r5/2026-05-13 --json-out ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json --json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-after-passive-sniff-payload-coverage.json --md-out ci/hardware/moza-r5/2026-05-13/index.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-current/native-visible-after-passive-sniff-payload-coverage.json --json; if ($LASTEXITCODE -eq 4) { exit 0 } else { throw \"expected native-visible verifier to remain blocked\" }",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "passive-sniff-usbcom-payload-extraction"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-passive-sniff-usbcom-payload-extraction"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json"
claim_boundary = "No-output passive sniff parser/export improvement only; extracts USB CDC/serial payloads from checked-in Pit House captures as non-claiming protocol candidates without opening HID, opening serial, sending read-only queries, sending hardware output, creating authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, or release-ready claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_summary_extracts_usbcom_host_to_device_payloads -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_summary -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_protocol_evidence_review -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-summary --pcapng target/sniff/pit-house-open-idle/capture.pcapng --vendor 0x346E --product 0x0004 --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-summary.json --md-out target/sniff/pit-house-open-idle/sniff-summary.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-summary --pcapng target/sniff/pit-house-full-controls/capture.pcapng --vendor 0x346E --product 0x0004 --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-summary.json --md-out target/sniff/pit-house-full-controls/sniff-summary.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-bundle --plan ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-plan.json --receipt ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-receipt.json --summary ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-summary.json --operator-notes target/sniff/pit-house-open-idle/operator-notes.md --out target/sniff/pit-house-open-idle/openracing-sniff-bundle.zip --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-bundle-manifest.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-bundle --plan ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-plan.json --receipt ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-receipt.json --summary ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-summary.json --operator-notes target/sniff/pit-house-full-controls/operator-notes.md --out target/sniff/pit-house-full-controls/openracing-sniff-bundle.zip --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-bundle-manifest.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza vendor-protocol-evidence-review --lane ci/hardware/moza-r5/2026-05-13 --json-out ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json --json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-after-passive-sniff-usbcom-payload-extraction.json --md-out ci/hardware/moza-r5/2026-05-13/index.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-current/native-visible-after-passive-sniff-usbcom-payload-extraction.json --json; if ($LASTEXITCODE -eq 4) { exit 0 } else { throw \"expected native-visible verifier to remain blocked\" }",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "passive-sniff-usbcom-frame-shape-review"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-passive-sniff-usbcom-frame-shape-review"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json"
claim_boundary = "No-output passive sniff frame-shape review only; parses extracted Pit House USB CDC host-to-device payloads into length-prefixed 0x7E serial-frame candidate counts, checksum-valid counts, and tuple IDs without opening HID, opening serial, sending read-only queries, sending hardware output, creating authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, or semantic command/sendability claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_summary_extracts_usbcom_host_to_device_payloads -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_summary -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_protocol_evidence_review -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-summary --pcapng target/sniff/pit-house-open-idle/capture.pcapng --vendor 0x346E --product 0x0004 --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-summary.json --md-out target/sniff/pit-house-open-idle/sniff-summary.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-summary --pcapng target/sniff/pit-house-full-controls/capture.pcapng --vendor 0x346E --product 0x0004 --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-summary.json --md-out target/sniff/pit-house-full-controls/sniff-summary.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-bundle --plan ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-plan.json --receipt ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-receipt.json --summary ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-summary.json --operator-notes target/sniff/pit-house-open-idle/operator-notes.md --out target/sniff/pit-house-open-idle/openracing-sniff-bundle.zip --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-bundle-manifest.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-bundle --plan ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-plan.json --receipt ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-receipt.json --summary ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-summary.json --operator-notes target/sniff/pit-house-full-controls/operator-notes.md --out target/sniff/pit-house-full-controls/openracing-sniff-bundle.zip --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-bundle-manifest.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza vendor-protocol-evidence-review --lane ci/hardware/moza-r5/2026-05-13 --json-out ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json --json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-after-passive-sniff-usbcom-frame-shape-review.json --md-out ci/hardware/moza-r5/2026-05-13/index.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-current/native-visible-after-passive-sniff-usbcom-frame-shape-review.json --json; if ($LASTEXITCODE -eq 4) { exit 0 } else { throw \"expected native-visible verifier to remain blocked\" }",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "passive-sniff-tuple-registry-coverage"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-passive-sniff-tuple-registry-coverage"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json"
claim_boundary = "No-output passive tuple registry coverage only; compares checked-in Pit House 0x7E tuple IDs to the semantic vendor command registry and fences read-only, commandless, and unknown tuples without opening HID, opening serial, sending read-only queries, sending hardware output, creating authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, or tuple sendability claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_protocol_evidence_review -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza vendor-protocol-evidence-review --lane ci/hardware/moza-r5/2026-05-13 --json-out ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json --json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-after-passive-tuple-registry-review.json --md-out ci/hardware/moza-r5/2026-05-13/index.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-current/native-visible-after-passive-tuple-registry-review.json --json; if ($LASTEXITCODE -eq 4) { exit 0 } else { throw \"expected native-visible verifier to remain blocked\" }",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "passive-sniff-tuple-frequency-review"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-passive-sniff-tuple-frequency-review"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json"
claim_boundary = "No-output passive tuple frequency review only; ranks checked-in Pit House 0x7E tuple counts and highest-frequency unknown commanded tuples without opening HID, opening serial, sending read-only queries, sending hardware output, creating authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic decode, or tuple sendability claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_protocol_evidence_review -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza vendor-protocol-evidence-review --lane ci/hardware/moza-r5/2026-05-13 --json-out ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json --json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-after-passive-tuple-frequency-review.json --md-out ci/hardware/moza-r5/2026-05-13/index.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-current/native-visible-after-passive-tuple-frequency-review.json --json; if ($LASTEXITCODE -eq 4) { exit 0 } else { throw \"expected native-visible verifier to remain blocked\" }",
  "cargo test --locked -p wheelctl --bin wheelctl checked_in_moza_lane_index_matches_artifact_index_renderer -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "passive-sniff-decode-priority-navigation"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-passive-sniff-decode-priority-navigation"
current_pointer = "ci/hardware/moza-r5/2026-05-13/index.md"
claim_boundary = "No-output decode-priority navigation only; surfaces frequency-ranked unknown commanded Pit House 0x7E tuples in artifact-index and bench-wizard without opening HID, opening serial, sending read-only queries, sending hardware output, creating authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic decode, registry promotion, or tuple sendability claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_authority_navigation_surfaces_decode_priority_without_claims -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl artifact_index -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-after-decode-priority-navigation.json --md-out ci/hardware/moza-r5/2026-05-13/index.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/bench-wizard-after-decode-priority-navigation.json --md-out target/moza-current/bench-wizard-after-decode-priority-navigation.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-current/native-visible-after-decode-priority-navigation.json --json; if ($LASTEXITCODE -eq 4) { exit 0 } else { throw \"expected native-visible verifier to remain blocked\" }",
  "cargo test --locked -p wheelctl --bin wheelctl checked_in_moza_lane_index_matches_artifact_index_renderer -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "passive-sniff-tuple-sample-fixtures"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-passive-sniff-tuple-sample-fixtures"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json"
claim_boundary = "No-output passive tuple sample fixture preservation only; stores bounded sample frames for checked-in Pit House 0x7E tuple IDs and surfaces decode-candidate sample counts without opening HID, opening serial, sending read-only queries, sending hardware output, creating authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic decode, registry promotion, or tuple sendability claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_summary_extracts_usbcom_host_to_device_payloads -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_summary -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_protocol_evidence_review -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-summary --pcapng target/sniff/pit-house-open-idle/capture.pcapng --vendor 0x346E --product 0x0004 --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-summary.json --md-out target/sniff/pit-house-open-idle/sniff-summary.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-summary --pcapng target/sniff/pit-house-full-controls/capture.pcapng --vendor 0x346E --product 0x0004 --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-summary.json --md-out target/sniff/pit-house-full-controls/sniff-summary.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-bundle --plan ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-plan.json --receipt ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-receipt.json --summary ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-summary.json --operator-notes target/sniff/pit-house-open-idle/operator-notes.md --out target/sniff/pit-house-open-idle/openracing-sniff-bundle.zip --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-bundle-manifest.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-bundle --plan ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-plan.json --receipt ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-receipt.json --summary ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-summary.json --operator-notes target/sniff/pit-house-full-controls/operator-notes.md --out target/sniff/pit-house-full-controls/openracing-sniff-bundle.zip --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-bundle-manifest.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza vendor-protocol-evidence-review --lane ci/hardware/moza-r5/2026-05-13 --json-out ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json --json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-after-passive-tuple-samples.json --md-out ci/hardware/moza-r5/2026-05-13/index.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-current/native-visible-after-passive-tuple-samples.json --json; if ($LASTEXITCODE -eq 4) { exit 0 } else { throw \"expected native-visible verifier to remain blocked\" }",
  "cargo test --locked -p wheelctl --bin wheelctl checked_in_moza_lane_index_matches_artifact_index_renderer -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "passive-sniff-observed-tuple-decoder-regression"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-passive-sniff-observed-tuple-decoder-regression"
current_pointer = "crates/hid-moza-protocol/src/serial/frame.rs"
claim_boundary = "No-output passive tuple decoder regression only; validates checked-in Pit House sample frames as checksum-valid observed frames while keeping unknown tuples out of the semantic registry and without opening HID, opening serial, sending read-only queries, sending hardware output, creating authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic command, registry promotion, or tuple sendability claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_passive_tuple_samples -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_serial_codec_fixtures -- --nocapture",
  "cargo clippy --locked -p racing-wheel-hid-moza-protocol --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "passive-sniff-tuple-sequence-regression"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-passive-sniff-tuple-sequence-regression"
current_pointer = "crates/hid-moza-protocol/tests/vendor_passive_tuple_samples.rs"
claim_boundary = "No-output passive tuple packet-order regression only; validates repeated ordering hints in checked-in Pit House sample frames while keeping unknown tuples out of the semantic registry and without opening HID, opening serial, sending read-only queries, sending hardware output, creating authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic command, registry promotion, or tuple sendability claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_passive_tuple_samples -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_serial_codec_fixtures -- --nocapture",
  "cargo clippy --locked -p racing-wheel-hid-moza-protocol --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "passive-sniff-tuple-payload-shape-review"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-passive-sniff-tuple-payload-shape-review"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json"
claim_boundary = "No-output passive tuple payload-shape review only; summarizes empty and zero-filled payload morphology for checked-in Pit House decode-candidate sample frames while keeping unknown tuples out of the semantic registry and without opening HID, opening serial, sending read-only queries, sending hardware output, creating authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic command, registry promotion, or tuple sendability claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_protocol_evidence_review -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_authority_navigation_surfaces_decode_priority_without_claims -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_passive_tuple_samples -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza vendor-protocol-evidence-review --lane ci/hardware/moza-r5/2026-05-13 --json-out ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json --json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-after-passive-payload-shapes.json --md-out ci/hardware/moza-r5/2026-05-13/index.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-current/native-visible-after-passive-payload-shapes.json --json; if ($LASTEXITCODE -eq 4) { exit 0 } else { throw \"expected native-visible verifier to remain blocked\" }",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo clippy --locked -p racing-wheel-hid-moza-protocol --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "passive-sniff-tuple-packet-group-review"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-passive-sniff-tuple-packet-group-review"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json"
claim_boundary = "No-output passive tuple packet-group review only; summarizes packet-local grouping and repeated contiguous motifs for checked-in Pit House decode-candidate sample frames while keeping unknown tuples out of the semantic registry and without opening HID, opening serial, sending read-only queries, sending hardware output, creating authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic command, registry promotion, or tuple sendability claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_protocol_evidence_review -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_authority_navigation_surfaces_decode_priority_without_claims -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_passive_tuple_samples -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza vendor-protocol-evidence-review --lane ci/hardware/moza-r5/2026-05-13 --json-out ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json --json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-after-passive-packet-groups.json --md-out ci/hardware/moza-r5/2026-05-13/index.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/bench-wizard-after-passive-packet-groups.json --md-out target/moza-current/bench-wizard-after-passive-packet-groups.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-current/native-visible-after-passive-packet-groups.json --json; if ($LASTEXITCODE -eq 4) { exit 0 } else { throw \"expected native-visible verifier to remain blocked\" }",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo clippy --locked -p racing-wheel-hid-moza-protocol --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "passive-sniff-payload-gap-examples"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-passive-sniff-payload-gap-examples"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json"
claim_boundary = "No-output passive payload export gap locator review only; preserves bounded packet/frame examples for host-to-device data-length packets that still lack extracted payload bytes without opening HID, opening serial, sending read-only queries, sending hardware output, creating authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic decode, registry promotion, or tuple sendability claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl hardware_sniff_summary -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_protocol_evidence_review -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_authority_navigation_surfaces_decode_priority_without_claims -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-summary --pcapng target/sniff/pit-house-open-idle/capture.pcapng --vendor 0x346E --product 0x0004 --include-payload-samples --max-samples-per-report 2 --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-summary.json --md-out target/sniff/pit-house-open-idle/sniff-summary.md",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-summary --pcapng target/sniff/pit-house-full-controls/capture.pcapng --vendor 0x346E --product 0x0004 --include-payload-samples --max-samples-per-report 2 --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-summary.json --md-out target/sniff/pit-house-full-controls/sniff-summary.md",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json hardware sniff-bundle --plan ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-plan.json --receipt ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-receipt.json --summary ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-summary.json --operator-notes target/sniff/pit-house-open-idle/operator-notes.md --include-pcapng target/sniff/pit-house-open-idle/capture.pcapng --out target/sniff/pit-house-open-idle/openracing-sniff-bundle.zip --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-open-idle/sniff-bundle-manifest.json",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json hardware sniff-bundle --plan ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-plan.json --receipt ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-receipt.json --summary ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-summary.json --operator-notes target/sniff/pit-house-full-controls/operator-notes.md --include-pcapng target/sniff/pit-house-full-controls/capture.pcapng --out target/sniff/pit-house-full-controls/openracing-sniff-bundle.zip --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-full-controls/sniff-bundle-manifest.json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza vendor-protocol-evidence-review --lane ci/hardware/moza-r5/2026-05-13 --json-out ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json --json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-after-passive-payload-gap.json --md-out ci/hardware/moza-r5/2026-05-13/index.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/bench-wizard-after-passive-payload-gap.json --md-out target/moza-current/bench-wizard-after-passive-payload-gap.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-current/native-visible-after-passive-payload-gap.json --json; if ($LASTEXITCODE -eq 4) { exit 0 } else { throw \"expected native-visible verifier to remain blocked\" }",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "passive-sniff-semantic-hypothesis-review"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-passive-sniff-semantic-hypothesis-review"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json"
claim_boundary = "No-output passive semantic hypothesis review only; preserves low-confidence pattern-only hypotheses for the highest-frequency unknown commanded Pit House tuples while keeping them unknown_do_not_send and without opening HID, opening serial, sending read-only queries, sending hardware output, creating authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic decode, registry promotion, or tuple sendability claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_protocol_evidence_review -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_authority_navigation_surfaces_decode_priority_without_claims -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_passive_tuple_samples -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza vendor-protocol-evidence-review --lane ci/hardware/moza-r5/2026-05-13 --json-out ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json --json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-after-passive-semantic-hypotheses.json --md-out ci/hardware/moza-r5/2026-05-13/index.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/bench-wizard-after-passive-semantic-hypotheses.json --md-out target/moza-current/bench-wizard-after-passive-semantic-hypotheses.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-current/native-visible-after-passive-semantic-hypotheses.json --json; if ($LASTEXITCODE -eq 4) { exit 0 } else { throw \"expected native-visible verifier to remain blocked\" }",
  "cargo test --locked -p wheelctl --bin wheelctl checked_in_moza_lane_index_matches_artifact_index_renderer -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo clippy --locked -p racing-wheel-hid-moza-protocol --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "passive-sniff-semantic-correlation-plan"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-passive-sniff-semantic-correlation-plan"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json"
claim_boundary = "No-output passive semantic correlation planning only; converts low-confidence tuple hypotheses into named passive capture requirements while keeping every tuple unknown_do_not_send and without opening HID, opening serial, sending read-only queries, sending hardware output, creating authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic decode, registry promotion, or tuple sendability claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_protocol_evidence_review -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_authority_navigation_surfaces_decode_priority_without_claims -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_passive_tuple_samples -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza vendor-protocol-evidence-review --lane ci/hardware/moza-r5/2026-05-13 --json-out ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json --json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-after-passive-semantic-correlation.json --md-out ci/hardware/moza-r5/2026-05-13/index.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/bench-wizard-after-passive-semantic-correlation.json --md-out target/moza-current/bench-wizard-after-passive-semantic-correlation.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-current/native-visible-after-passive-semantic-correlation.json --json; if ($LASTEXITCODE -eq 4) { exit 0 } else { throw \"expected native-visible verifier to remain blocked\" }",
  "cargo test --locked -p wheelctl --bin wheelctl checked_in_moza_lane_index_matches_artifact_index_renderer -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo clippy --locked -p racing-wheel-hid-moza-protocol --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "passive-sniff-setting-change-notes-requirements"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-passive-sniff-setting-change-notes-requirements"
current_pointer = "ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/sniff-plan.json"
claim_boundary = "No-output passive capture handoff hardening only; the Pit House setting-change plan and notes template require exact setting/value/restore evidence without opening HID, opening serial, sending read-only queries, sending hardware output, creating authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic decode, registry promotion, or tuple sendability claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl hardware_sniff -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json hardware sniff-plan --family moza-r5 --scenario pit-house-setting-change --lane ci/hardware/moza-r5/2026-05-13 --operator Steven --device-note \"Moza R5 PID 0x0004 with KS/ES wheels, SR-P pedals, and HBP handbrake attached through the R5 hub\" --capture-tool usbpcap --capture-tool wireshark --capture-tool tshark --platform-hint windows --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/sniff-plan.json",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json hardware sniff-notes-template --plan ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/sniff-plan.json --out target/sniff/pit-house-setting-change/operator-notes.md --json",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "bench-wizard-passive-correlation-capture-handoff"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-bench-wizard-passive-correlation-capture-handoff"
current_pointer = "crates/cli/src/commands/moza.rs"
claim_boundary = "No-output operator navigation only; after vendor protocol evidence review is recorded, bench-wizard routes the next missing passive semantic-correlation scenario to the existing capture handoff without opening HID, opening serial, sending read-only queries, sending hardware output, creating authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic decode, registry promotion, or tuple sendability claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_protocol_review_routes_next_missing_passive_correlation_capture -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard_sniff_next_operator_commands_parse -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/bench-wizard-passive-correlation-handoff.json --md-out target/moza-current/bench-wizard-passive-correlation-handoff.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-current/native-visible-after-passive-correlation-handoff.json --json; if ($LASTEXITCODE -ne 4) { throw \"expected native-visible verifier to remain blocked\" }; $global:LASTEXITCODE = 0",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "hardware-doctor-usbpcap-device-hints"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-hardware-doctor-usbpcap-device-hints"
current_pointer = "crates/cli/src/commands/hardware.rs"
claim_boundary = "No-output capture-tool diagnostics only; hardware doctor surfaces USBPcap extcap Moza device-filter hints for passive capture setup without opening HID, opening serial, sending read-only queries, sending hardware output, creating authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic decode, registry promotion, or tuple sendability claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl usbpcap -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard_sniff_next_operator_commands_parse -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json hardware doctor --json-out target/moza-current/hardware-doctor-usbpcap-device-hints.json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/bench-wizard-usbpcap-device-hints.json --md-out target/moza-current/bench-wizard-usbpcap-device-hints.md --json",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "sniff-notes-usbpcap-device-hints"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-sniff-notes-usbpcap-device-hints"
current_pointer = "crates/cli/src/commands/hardware.rs"
claim_boundary = "No-output operator notes only; sniff-notes-template can embed observe-only hardware doctor USBPcap Moza device-filter hints for passive capture reproducibility without creating a pcap, sniff receipt, summary, bundle, HID open, serial open, query send, hardware output, authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic decode, registry promotion, or tuple sendability claim."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_notes_template -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard_sniff_next_operator_commands_parse -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json hardware sniff-notes-template --plan ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/sniff-plan.json --hardware-doctor target/moza-current/passive-sniff-capture-hardware-doctor.json --out target/sniff/pit-house-setting-change/operator-notes.md",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "sniff-notes-usbpcap-capture-command"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-sniff-notes-usbpcap-capture-command"
current_pointer = "crates/cli/src/commands/hardware.rs"
claim_boundary = "No-output operator notes only; sniff-notes-template can render an exact external USBPcapCMD capture command from observe-only hardware doctor USBPcap hints without creating a pcap, sniff receipt, summary, bundle, HID open, serial open, query send, hardware output, authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic decode, registry promotion, or tuple sendability claim."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_notes_template -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json hardware doctor --json-out target/moza-current/passive-sniff-capture-hardware-doctor.json",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json hardware sniff-notes-template --plan ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/sniff-plan.json --hardware-doctor target/moza-current/passive-sniff-capture-hardware-doctor.json --out target/sniff/pit-house-setting-change/operator-notes.md",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "sniff-notes-template-json-out"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-sniff-notes-template-json-out"
current_pointer = "crates/cli/src/commands/hardware.rs"
claim_boundary = "No-output operator notes receipt plumbing only; sniff-notes-template can write its existing non-claiming JSON receipt to --json-out and bench-wizard can surface that local receipt path without creating a pcap, sniff receipt, summary, bundle, HID open, serial open, query send, hardware output, authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic decode, registry promotion, or tuple sendability claim."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_notes_template -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard_sniff_next_operator_commands_parse -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json hardware sniff-notes-template --plan ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/sniff-plan.json --hardware-doctor target/moza-current/passive-sniff-capture-hardware-doctor.json --out target/sniff/pit-house-setting-change/operator-notes.md --json-out target/sniff/pit-house-setting-change/sniff-notes-template-receipt.json",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "sniff-bundle-notes-template-receipt"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-sniff-bundle-notes-template-receipt"
current_pointer = "crates/cli/src/commands/hardware.rs"
claim_boundary = "No-output bundle handoff plumbing only; sniff-bundle can optionally include the non-claiming sniff-notes-template JSON receipt and bench-wizard can surface that local receipt path without creating a pcap, sniff receipt, summary, HID open, serial open, query send, hardware output, authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic decode, registry promotion, or tuple sendability claim."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_bundle -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl parse_hardware_sniff_bundle -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard_sniff_next_operator_commands_parse -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/bench-wizard-sniff-bundle-notes-receipt.json --md-out target/moza-current/bench-wizard-sniff-bundle-notes-receipt.md --json",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "sniff-bundle-notes-template-receipt-validation"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-sniff-bundle-notes-template-receipt-validation"
current_pointer = "crates/cli/src/commands/hardware.rs"
claim_boundary = "No-output bundle validation hardening only; sniff-bundle validates that an optional sniff-notes-template JSON receipt is non-claiming and matches the same plan and operator-notes path before bundling, without creating a pcap, sniff receipt, summary, HID open, serial open, query send, hardware output, authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic decode, registry promotion, or tuple sendability claim."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_bundle -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl parse_hardware_sniff_bundle -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard_sniff_next_operator_commands_parse -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/bench-wizard-sniff-bundle-notes-receipt-validation.json --md-out target/moza-current/bench-wizard-sniff-bundle-notes-receipt-validation.md --json",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "sniff-summary-usb-setup-payload-gaps"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-sniff-summary-usb-setup-payload-gaps"
current_pointer = "crates/cli/src/commands/hardware.rs"
claim_boundary = "No-output sniff-summary parser hardening only; USB control setup stages are not counted as missing vendor payload bytes when no payload field is present. This does not create or regenerate pcap, sniff receipt, sniff summary, bundle, HID open, serial open, query send, hardware output, authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic decode, registry promotion, or tuple sendability claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_summary -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "bench-wizard-usbpcap-capture-command"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-bench-wizard-usbpcap-capture-command"
current_pointer = "crates/cli/src/commands/moza.rs"
claim_boundary = "No-output operator navigation only; bench-wizard surfaces an external USBPcap capture command template for the next passive sniff scenario without creating a pcap, sniff receipt, summary, bundle, HID open, serial open, query send, OpenRacing hardware output, authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic decode, registry promotion, or tuple sendability claim."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard_sniff_next_operator_commands_parse -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_protocol_review_routes_next_missing_passive_correlation_capture -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/bench-wizard-usbpcap-capture-command.json --md-out target/moza-current/bench-wizard-usbpcap-capture-command.md --json",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "sniff-bundle-setting-change-notes-validation"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-sniff-bundle-setting-change-notes-validation"
current_pointer = "crates/cli/src/commands/hardware.rs"
claim_boundary = "No-output bundle validation hardening only; sniff-bundle rejects Pit House setting-change bundles whose operator notes leave the exact setting, starting value, ending value, or restore status blank, without creating a pcap, sniff receipt, summary, bundle, HID open, serial open, query send, hardware output, authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic decode, registry promotion, or tuple sendability claim."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_bundle -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_notes_template -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "hardware-doctor-usbpcap-active-processes"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-hardware-doctor-usbpcap-active-processes"
current_pointer = "crates/cli/src/commands/hardware.rs"
claim_boundary = "No-output capture-precondition diagnostics only; hardware doctor and sniff-notes-template surface active USBPcapCMD processes so stale captures are visible before a passive scenario without creating a pcap, sniff receipt, summary, bundle, HID open, serial open, query send, hardware output, authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic decode, registry promotion, or tuple sendability claim."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl usbpcap -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_notes_template -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json hardware doctor --json-out target/moza-current/hardware-doctor-usbpcap-active-processes.json",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json hardware sniff-notes-template --plan ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/sniff-plan.json --hardware-doctor target/moza-current/hardware-doctor-usbpcap-active-processes.json --out target/sniff/pit-house-setting-change/operator-notes.md --json-out target/sniff/pit-house-setting-change/sniff-notes-template-receipt.json",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "hardware-sniff-bounded-usbpcap-capture"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-hardware-sniff-bounded-usbpcap-capture"
current_pointer = "crates/cli/src/commands/hardware.rs"
claim_boundary = "No-output passive capture runner only; wheelctl can launch USBPcapCMD for a bounded duration and record a non-claiming local capture receipt without opening HID, opening serial, sending read-only queries, sending hardware output, creating sniff receipt/summary/bundle evidence, authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic decode, registry promotion, or tuple sendability claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_capture -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_notes_template -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl parse_hardware_sniff_capture -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "moza-bench-wizard-bounded-sniff-capture-template"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-moza-bench-wizard-bounded-sniff-capture-template"
current_pointer = "crates/cli/src/commands/moza.rs"
claim_boundary = "No-output operator navigation only; bench-wizard and artifact-index surface the bounded wheelctl hardware sniff-capture template as the preferred passive USBPcap handoff without opening HID, opening serial, sending read-only queries, sending hardware output, creating sniff receipt/summary/bundle evidence, authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic decode, registry promotion, or tuple sendability claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard_sniff_next_operator_commands_parse -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard_points_diagnosed_attempt_03_to_passive_sniff_capture -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl passive_sniff -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/bench-wizard-current.json --md-out target/moza-current/bench-wizard-current.md",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-current.json --md-out target/moza-current/artifact-index-current.md",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "hardware-sniff-capture-tool-logs"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-hardware-sniff-capture-tool-logs"
current_pointer = "crates/cli/src/commands/hardware.rs"
claim_boundary = "No-output passive capture diagnostics only; sniff-capture preserves USBPcapCMD stdout/stderr logs beside the local scratch capture and records their paths/sizes in the local capture receipt without creating sniff receipt/summary/bundle evidence, HID open, serial open, query send, hardware output, authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic decode, registry promotion, or tuple sendability claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_capture -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "classify-low-yield-setting-change-capture"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-classify-low-yield-setting-change-capture"
current_pointer = "ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/low-yield-capture-classification.json"
claim_boundary = "No-output classification and validation hardening only; the 355-byte six-packet setting-change capture is recorded as low-yield/incomplete, failed summaries stay present-not-accepted, and missing/non-affirmative restore status cannot be bundled as accepted setting-change evidence. No raw pcap, HID open, serial open, query send, hardware output, authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic decode, registry promotion, or tuple sendability claim is created."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_bundle -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl passive_sniff -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard_sniff_next_operator_commands_parse -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-low-yield-setting-change.json --md-out ci/hardware/moza-r5/2026-05-13/index.md",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/bench-wizard-low-yield-setting-change.json --md-out target/moza-current/bench-wizard-low-yield-setting-change.md",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "record-pit-house-setting-change-capture"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-record-pit-house-setting-change-capture"
current_pointer = "ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/sniff-summary.json"
claim_boundary = "No-output passive correlation evidence only; the repeat Pit House setting-change capture records derived receipt, summary, bundle manifest, and operator notes without raw pcap, hardware output, native-control, native-visible, smoke-ready, release-ready, coexistence, simulator, semantic decode, registry promotion, authorization, or tuple sendability claims."
commands = [
  "cargo test --locked -p wheelctl --bin wheelctl sniff_bundle -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl passive_sniff -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard_sniff_next_operator_commands_parse -- --nocapture",
  "cargo test -p racing-wheel-hid-moza-protocol --test vendor_passive_tuple_samples --locked -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-setting-change-recorded.json --md-out ci/hardware/moza-r5/2026-05-13/index.md",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/bench-wizard-setting-change-recorded.json --md-out target/moza-current/bench-wizard-setting-change-recorded.md",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "hardware-sniff-capture-finalization-receipt"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-hardware-sniff-capture-finalization-receipt"
current_pointer = "crates/cli/src/commands/hardware.rs"
claim_boundary = "No-output passive capture helper hardening only; sniff-capture retries local pcap metadata/hash readback after USBPcapCMD exits and records finalization diagnostics in the local capture receipt without creating sniff receipt/summary/bundle evidence, raw pcap commit, HID open, serial open, query send, hardware output, authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic decode, registry promotion, or tuple sendability claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_capture -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl passive_sniff -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "hardware-sniff-usbpcap-selector-guard"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-hardware-sniff-usbpcap-selector-guard"
current_pointer = "crates/cli/src/commands/hardware.rs"
claim_boundary = "No-output passive capture selector guardrail only; sniff-capture can verify a requested USBPcap interface/device selector against a fresh observe-only hardware doctor Moza hint and marks stale or non-Moza selectors failed without creating sniff receipt/summary/bundle evidence, raw pcap commit, HID open, serial open, query send, hardware output, authorization, native-control, native-visible, smoke-ready, coexistence, simulator, firmware, release-ready, semantic decode, registry promotion, or tuple sendability claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_capture -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl passive_sniff -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard_sniff_next_operator_commands_parse -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "stage-simhub-open-idle-capture-handoff"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-stage-simhub-open-idle-capture-handoff"
current_pointer = "ci/hardware/sniff/moza-r5/2026-05-13/simhub-open-idle/sniff-plan.json"
claim_boundary = "No-output SimHub open-idle passive capture staging only; checked-in plan/operator-note/wizard/artifact-index handoffs route the next passive gap to simhub-open-idle and require fresh hardware-doctor USBPcap selector verification without launching SimHub, running a live capture, creating sniff receipt/summary/bundle evidence, committing raw pcap/zip/installer/binary artifacts, opening HID or serial, sending query/output/feature/configuration/firmware commands, authorizing hardware output, or claiming semantic decode, registry coverage, tuple sendability, native-control, native-visible, smoke-ready, simulator readiness, coexistence readiness, or release readiness."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl passive_sniff -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard_sniff_next_operator_commands_parse -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl artifact_index -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "classify-vendor-mode-enable-decode-candidates"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-classify-vendor-mode-enable-decode-candidates"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json"
claim_boundary = "No-output passive tuple mode/enable candidate classification only; derives low-confidence semantic questions for volatile FFB/session enable, standard PIDFF mode enable, game/control mode select, authority keepalive, and status query from checked-in passive tuple hypotheses while every candidate remains unknown_do_not_send with no semantic decode, registry promotion, tuple sendability, authorization, hardware output, native-control, native-visible, smoke-ready, simulator, coexistence, firmware, or release-ready claim."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_protocol_evidence_review -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_authority_navigation_surfaces_decode_priority_without_claims -- --nocapture",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza vendor-protocol-evidence-review --lane ci/hardware/moza-r5/2026-05-13 --json-out ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json --json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-mode-enable-candidates.json --md-out ci/hardware/moza-r5/2026-05-13/index.md",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "verify-vendor-mode-candidates-fake-transport"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-verify-vendor-mode-candidates-fake-transport"
current_pointer = "fixtures/moza/r5/vendor-fake-serial-transport.json"
claim_boundary = "No-output fake-transport containment only; representative passive mode/enable candidate frames are parsed and routed through software fake transport as unknown_do_not_send observations while command/send paths reject them, with no semantic decode, registry promotion, tuple sendability, authorization, hardware output, native-control, native-visible, smoke-ready, simulator, coexistence, firmware, high-torque, or release-ready claim."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_protocol_evidence_review -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo clippy --locked -p racing-wheel-hid-moza-protocol --all-targets --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "stage-read-only-vendor-status-mode-matrix"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-stage-read-only-vendor-status-mode-matrix"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix-plan.json"
claim_boundary = "No-output read-only status/mode matrix staging only; records the expected matrix fields, preconditions, allowed read-only status commands, and blocked unknown mode/enable candidate groups without opening HID, opening serial, sending read-only queries, sending PIDFF/output/feature/configuration/firmware commands, creating authorization, promoting registry/sendability, or claiming native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, or release-ready."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_protocol_evidence_review -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_fake_serial_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo clippy --locked -p racing-wheel-hid-moza-protocol --all-targets --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "record-read-only-vendor-status-mode-matrix"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-record-read-only-vendor-status-mode-matrix"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix.json"
claim_boundary = "Read-only serial status/mode matrix evidence only; COM4 R5 identity was verified and nine registry-approved read-only status queries were sent, but zero responses decoded, unknown mode/safety status still blocks authority planning, and no HID, PIDFF, output, feature, configuration, firmware, DFU, high-torque, authorization, registry promotion, tuple sendability, native-control, native-visible, smoke-ready, simulator, coexistence, or release-ready claim is created."
commands = [
  "wheelctl hardware doctor --json --json-out target/moza-current/vendor-status-precondition-hardware-doctor.json",
  "wheelctl moza vendor-status-probe --serial-port COM4 --baud-rate 115200 --timeout-ms 250 --confirm-read-only-query --json-out target/moza-current/vendor-status-mode-matrix-live.json --json",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_protocol_evidence_review -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_fake_serial_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo clippy --locked -p racing-wheel-hid-moza-protocol --all-targets --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "diagnose-read-only-serial-response-framing"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-diagnose-read-only-serial-response-framing"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-framing-diagnosis.json"
claim_boundary = "No-output diagnosis of the stored read-only status/mode matrix only; classifies the nine captured response frames as repeated 0x0E/0x71/0x05 ASCII NRFloss/recvGap diagnostic stream frames with one desynchronized partial frame, keeps the #73 failed-closed receipt intact, opens no HID or serial device, sends no read-only query/output/configuration/firmware/PIDFF command, and creates no authorization, semantic decode, registry promotion, tuple sendability, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, or release-ready claim."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-framing-diagnosis --status-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix.json --json-out target/moza-current/vendor-status-framing-diagnosis.json --overwrite",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_status_probe -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo clippy --locked -p racing-wheel-hid-moza-protocol --all-targets --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "demux-read-only-status-response-stream"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-demux-read-only-status-response-stream"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix-demux.json"
claim_boundary = "Read-only serial response demux only; the probe skips diagnostic stream frames, accepts the observed response-side group/device tuple transform for registry status replies, and records seven decoded read-only responses plus two failed-closed responses without opening HID, sending PIDFF/output/feature/configuration/firmware commands, authorizing output, promoting registry sendability, or claiming native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, or release-ready."
commands = [
  "wheelctl hardware doctor --json --json-out target/moza-current/read-only-stream-demux-hardware-doctor.json",
  "wheelctl moza vendor-status-probe --serial-port COM4 --baud-rate 115200 --timeout-ms 250 --confirm-read-only-query --json-out target/moza-current/vendor-status-mode-matrix-demux-live-v2.json --json",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo clippy --locked -p racing-wheel-hid-moza-protocol --all-targets --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "correlate-read-only-authority-status-replies"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-correlate-read-only-authority-status-replies"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-reply-correlation-diagnosis.json"
claim_boundary = "Read-only authority-status reply correlation only; records a targeted two-command COM4 read-only probe and offline scanned-frame diagnosis for estop_get_ffb and main_misc_get_ffb_status, decodes zero authority-state replies, classifies diagnostic telemetry and one response-like command mismatch, and keeps hardware output authorization, registry promotion, tuple sendability, semantic decode, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, and release-ready claims false."
commands = [
  "wheelctl hardware doctor --json --json-out target/moza-current/status-reply-correlation-hardware-doctor.json",
  "wheelctl moza vendor-status-probe --serial-port COM4 --baud-rate 115200 --timeout-ms 1000 --command estop_get_ffb --command main_misc_get_ffb_status --confirm-read-only-query --json-out target/moza-current/vendor-status-reply-correlation-targeted.json --json",
  "wheelctl moza vendor-status-framing-diagnosis --status-probe ci/hardware/moza-r5/2026-05-13/vendor-status-reply-correlation-targeted.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-reply-correlation-diagnosis.json --overwrite --json",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_framing_diagnosis -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_status_probe -- --nocapture",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "extend-read-only-authority-status-scan-window"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-extend-read-only-authority-status-scan-window"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-extended-scan-diagnosis.json"
claim_boundary = "Read-only authority-status extended scan only; adds a bounded --max-response-frames-per-query option and records a targeted COM4 read-only probe for estop_get_ffb and main_misc_get_ffb_status with a 64-frame scan cap, still decoding zero authority-state replies and keeping hardware output authorization, registry promotion, tuple sendability, semantic decode, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, and release-ready claims false."
commands = [
  "wheelctl hardware doctor --json --json-out target/moza-current/status-extended-scan-hardware-doctor.json",
  "wheelctl moza vendor-status-probe --serial-port COM4 --baud-rate 115200 --timeout-ms 1000 --max-response-frames-per-query 64 --command estop_get_ffb --command main_misc_get_ffb_status --confirm-read-only-query --json-out target/moza-current/vendor-status-extended-scan-targeted.json --json",
  "wheelctl moza vendor-status-framing-diagnosis --status-probe target/moza-current/vendor-status-extended-scan-targeted.json --json-out target/moza-current/vendor-status-extended-scan-diagnosis.json --overwrite --json",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_framing_diagnosis -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_status_probe -- --nocapture",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "classify-zero-length-authority-status-reply"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-classify-zero-length-authority-status-reply"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-extended-scan-diagnosis.json"
claim_boundary = "Stored-receipt read-only framing diagnosis only; reclassifies the scanned 7E00A1214D frame from the extended COM4 probe as a checksum-valid zero-length response-like ACK-only/no-payload candidate for 0xA1/0x21/no_command, decodes zero authority-state replies, and keeps hardware output authorization, registry promotion, tuple sendability, semantic decode, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, and release-ready claims false."
commands = [
  "wheelctl moza vendor-status-framing-diagnosis --status-probe ci/hardware/moza-r5/2026-05-13/vendor-status-extended-scan-targeted.json --json-out target/moza-current/vendor-status-zero-length-diagnosis-from-extended.json --overwrite --json",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_framing_diagnosis -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_status_probe -- --nocapture",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "classify-ack-only-authority-status-reply"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-classify-ack-only-authority-status-reply"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-ack-only-correlation-diagnosis.json"
claim_boundary = "Read-only ACK-only authority-status correlation only; after a fresh observe-only hardware doctor, reruns the targeted COM4 read-only probe for estop_get_ffb and main_misc_get_ffb_status, reclassifies 7E00A1214D as a checksum-valid zero-length ACK-only candidate for 0xA1/0x21/no_command with no command or status payload, decodes zero authority-state replies, leaves mode/safety unknown, and keeps hardware output authorization, registry promotion, tuple sendability, semantic decode, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, and release-ready claims false."
commands = [
  "wheelctl moza vendor-status-framing-diagnosis --status-probe ci/hardware/moza-r5/2026-05-13/vendor-status-extended-scan-targeted.json --json-out target/moza-current/vendor-status-ack-only-correlation-from-extended.json --overwrite --json",
  "wheelctl hardware doctor --json --json-out target/moza-current/ack-only-correlation-hardware-doctor.json",
  "wheelctl moza vendor-status-probe --serial-port COM4 --baud-rate 115200 --timeout-ms 1000 --max-response-frames-per-query 64 --command estop_get_ffb --command main_misc_get_ffb_status --confirm-read-only-query --json-out target/moza-current/vendor-status-ack-only-correlation-targeted.json --json",
  "wheelctl moza vendor-status-framing-diagnosis --status-probe target/moza-current/vendor-status-ack-only-correlation-targeted.json --json-out target/moza-current/vendor-status-ack-only-correlation-diagnosis.json --overwrite --json",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_framing_diagnosis -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_status_probe -- --nocapture",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "diagnose-authority-status-endpoint-specific-blocker"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-diagnose-authority-status-endpoint-specific-blocker"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-authority-endpoint-diagnosis.json"
claim_boundary = "No-output stored-receipt authority-status endpoint diagnosis only; compares the ACK-only targeted read-only probe against the demux baseline, rules out broad serial framing/ownership/timeout/line settings as the primary blocker because seven non-authority status replies decoded on the same serial lane, and keeps the authority-status blocker on endpoint/command correction with no hardware access, authorization, semantic decode, registry promotion, tuple sendability, hardware output, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, or release-ready claim."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-framing-diagnosis --status-probe ci/hardware/moza-r5/2026-05-13/vendor-status-ack-only-correlation-targeted.json --baseline-status-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix-demux.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-authority-endpoint-diagnosis.json --overwrite",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo clippy --locked -p racing-wheel-hid-moza-protocol --all-targets --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "classify-authority-status-endpoint-candidates"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-classify-authority-status-endpoint-candidates"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-endpoint-candidates.json"
claim_boundary = "No-output stored-receipt authority-status endpoint candidate review only; derives the expected payload response shape and passive tuple candidate groups from checked-in authority-endpoint diagnosis and protocol evidence review, keeps every candidate unknown_do_not_send and non-sendable, opens no HID or serial device, sends no read-only query, output, configuration, firmware, or PIDFF command, and creates no authorization, semantic decode, registry promotion, tuple sendability, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, or release-ready claim."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-endpoint-candidates --authority-endpoint-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-endpoint-diagnosis.json --protocol-evidence-review ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-endpoint-candidates.json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-after-status-endpoint-candidates.json --md-out ci/hardware/moza-r5/2026-05-13/index.md",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_endpoint_candidates -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_protocol_evidence_review -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "probe-authority-status-payload-fixtures"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-probe-authority-status-payload-fixtures"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-authority-payload-rerun-diagnosis.json"
claim_boundary = "Read-only authority-status payload fixture and targeted COM4 rerun only; adds fake decoder coverage for payload-bearing main_misc_get_ffb_status and estop_get_ffb response shapes, then records a targeted read-only live rerun that still decodes zero authority-state replies. Follow-up classifier correction narrows the blocker to authority-status endpoint/command mismatch, with no hardware output authorization, registry promotion, tuple sendability, semantic decode, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, or release-ready claim."
commands = [
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_status_probe -- --nocapture",
  "wheelctl hardware doctor --json --json-out target/moza-current/authority-status-payload-preflight-hardware-doctor.json",
  "wheelctl moza vendor-status-probe --serial-port COM4 --baud-rate 115200 --timeout-ms 1000 --command main_misc_get_ffb_status --command estop_get_ffb --confirm-read-only-query --json-out target/moza-current/vendor-status-authority-payload-rerun-targeted.json --json",
  "wheelctl moza vendor-status-framing-diagnosis --status-probe ci/hardware/moza-r5/2026-05-13/vendor-status-authority-payload-rerun-targeted.json --baseline-status-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix-demux.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-authority-payload-rerun-diagnosis.json --overwrite --json",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "classify-authority-payload-rerun-endpoint-blocker"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-classify-authority-payload-rerun-endpoint-blocker"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-authority-payload-rerun-diagnosis.json"
claim_boundary = "No-output stored-receipt classifier correction only; reclassifies the targeted authority-status payload rerun against the demux baseline as authority-status endpoint/command mismatch with diagnostic telemetry only, keeps broad serial ownership/framing/line settings ruled out as the primary blocker, decodes zero authority-state replies, and keeps hardware output authorization, registry promotion, tuple sendability, semantic decode, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, and release-ready claims false."
commands = [
  "wheelctl moza vendor-status-framing-diagnosis --status-probe ci/hardware/moza-r5/2026-05-13/vendor-status-authority-payload-rerun-targeted.json --baseline-status-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix-demux.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-authority-payload-rerun-diagnosis.json --overwrite --json",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "derive-payload-rerun-endpoint-candidates"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-derive-payload-rerun-endpoint-candidates"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-endpoint-candidates-from-payload-rerun.json"
claim_boundary = "No-output stored-receipt endpoint candidate review only; extends the endpoint-candidate planner to consume the debug-telemetry-only authority-status payload rerun, records diagnostic telemetry as endpoint-specific blocker evidence, keeps every passive candidate unknown_do_not_send and non-sendable, opens no HID or serial device, sends no read-only query or output/configuration/firmware/PIDFF command, and keeps authorization, semantic decode, registry promotion, tuple sendability, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, and release-ready claims false."
commands = [
  "wheelctl moza vendor-status-endpoint-candidates --authority-endpoint-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-payload-rerun-diagnosis.json --protocol-evidence-review ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-endpoint-candidates-from-payload-rerun.json --overwrite --json",
  "wheelctl moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-after-payload-endpoint-candidates.json --md-out ci/hardware/moza-r5/2026-05-13/index.md --json",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "contain-authority-status-endpoint-fixtures"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-contain-authority-status-endpoint-fixtures"
current_pointer = "fixtures/moza/r5/vendor-fake-serial-transport.json"
claim_boundary = "No-output fake-transport containment only; consumes the payload-rerun endpoint-candidate receipt and proves the passive authority-status endpoint candidates remain unknown_do_not_send, not payload-status matches, not corrected-probe-ready, and rejected by the command send path with no HID or serial open, read-only query send, output/configuration/firmware/PIDFF command, authorization, semantic decode, registry promotion, tuple sendability, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, release-ready, or wheel-movement claim."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo clippy --locked -p racing-wheel-hid-moza-protocol --all-targets --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "derive-authority-command-0x07-analogs"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-derive-authority-command-0x07-analogs"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-endpoint-candidates-from-payload-rerun.json"
claim_boundary = "No-output stored-receipt passive scan only; extends the authority-status endpoint candidate receipt to derive five unknown command-id 0x07 passive analog tuples from the checked-in protocol evidence review, including high-frequency 0x40/0x17/0x07, while keeping every analog not payload-status-matched, not read-only-probe-allowed, not sendable, and with no HID or serial open, read-only query send, output/configuration/firmware/PIDFF command, authorization, semantic decode, registry promotion, tuple sendability, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, release-ready, or wheel-movement claim."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "contain-authority-command-0x07-analogs"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-contain-authority-command-0x07-analogs"
current_pointer = "fixtures/moza/r5/vendor-fake-serial-transport.json"
claim_boundary = "No-output fake-transport containment only; consumes the passive command-id 0x07 analog tuples from the payload-rerun endpoint-candidate receipt, observes representative zero-payload fixture frames in fake transport, and proves all five analogs remain unknown_do_not_send, not payload-status matches, not read-only-probe-allowed, not sendable, and rejected by the command send path with no HID or serial open, read-only query send, output/configuration/firmware/PIDFF command, authorization, semantic decode, registry promotion, tuple sendability, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, release-ready, or wheel-movement claim."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_fake_serial_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo clippy --locked -p racing-wheel-hid-moza-protocol --all-targets --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "classify-authority-status-source-gap"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-classify-authority-status-source-gap"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-authority-source-gap.json"
claim_boundary = "No-output stored-evidence source-gap review only; consumes the payload-rerun endpoint-candidate receipt and passive protocol evidence review, records that no checked-in source currently identifies a payload-bearing authority-state status endpoint or equivalent reviewed status source, opens no HID or serial device, sends no read-only query or output/configuration/firmware/PIDFF command, and keeps corrected probe readiness, authorization planning, motion attempts, semantic decode, registry promotion, tuple sendability, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, release-ready, and wheel-movement claims false."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-authority-source-gap --endpoint-candidates ci/hardware/moza-r5/2026-05-13/vendor-status-endpoint-candidates-from-payload-rerun.json --protocol-evidence-review ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-authority-source-gap.json --overwrite",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_authority_source_gap -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "extract-device-to-host-response-samples"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-extract-device-to-host-response-samples"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json"
claim_boundary = "No-output stored passive sample extraction only; parses device-to-host report 0x7E payload samples from checked-in passive summaries, regenerates the source-gap receipt to record response-side tuple details as present but not correlated to a reviewed authority-state source, opens no HID or serial device, sends no read-only query or output/configuration/firmware/PIDFF command, and keeps corrected probe readiness, authorization planning, motion attempts, semantic decode, registry promotion, tuple sendability, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, release-ready, and wheel-movement claims false."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-protocol-evidence-review --lane ci/hardware/moza-r5/2026-05-13 --sniff-root ci/hardware/sniff/moza-r5/2026-05-13 --json-out ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-authority-source-gap --endpoint-candidates ci/hardware/moza-r5/2026-05-13/vendor-status-endpoint-candidates-from-payload-rerun.json --protocol-evidence-review ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-authority-source-gap.json --overwrite",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_protocol_evidence_review -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_authority_source_gap -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "correlate-response-source-samples"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-correlate-response-source-samples"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-response-source-correlation.json"
claim_boundary = "No-output stored passive response-source correlation only; consumes the source-gap receipt, endpoint candidates, and protocol evidence review to correlate response-shaped device-to-host samples to non-sendable passive question groups while keeping registry authority-status responses, command-id 0x07 analogs, corrected probe readiness, authorization planning, motion attempts, semantic decode, registry promotion, tuple sendability, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, release-ready, and wheel-movement claims false."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-response-source-correlation --source-gap ci/hardware/moza-r5/2026-05-13/vendor-status-authority-source-gap.json --endpoint-candidates ci/hardware/moza-r5/2026-05-13/vendor-status-endpoint-candidates-from-payload-rerun.json --protocol-evidence-review ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-response-source-correlation.json --overwrite",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "review-correlated-response-semantic-fixtures"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-review-correlated-response-semantic-fixtures"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-response-semantic-fixtures.json"
claim_boundary = "No-output stored passive response fixture review only; consumes the response-source correlation receipt and passive protocol evidence review, records fixture decoder coverage for correlated response samples, and preserves that all observed payloads are zero-filled/static and remain unknown_do_not_send, with no HID or serial open, no read-only query send, no output/configuration/firmware/PIDFF command, no corrected probe readiness, no authorization planning, no motion attempt, no semantic decode claim, no registry promotion, no tuple sendability, no native-control, no native-visible, no smoke-ready, no simulator, no coexistence, no high-torque, no release-ready, and no wheel-movement claim."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-response-semantic-fixtures --response-correlation ci/hardware/moza-r5/2026-05-13/vendor-status-response-source-correlation.json --protocol-evidence-review ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-response-semantic-fixtures.json --overwrite",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe_response_semantic_fixtures -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_response_semantic_fixtures -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo clippy --locked -p racing-wheel-hid-moza-protocol --all-targets --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "classify-payload-bearing-status-source-candidates"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-classify-payload-bearing-status-source-candidates"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-payload-source-candidates.json"
claim_boundary = "No-output stored passive payload-source candidate review only; consumes the response semantic fixture receipt and passive protocol evidence review, records four nonzero 0x8E device-to-host setting-change samples as unknown_do_not_send status-source questions, and keeps no HID or serial open, no read-only query send, no output/configuration/firmware/PIDFF command, no corrected probe readiness, no authorization planning, no motion attempt, no semantic decode claim, no registry promotion, no tuple sendability, no native-control, no native-visible, no smoke-ready, no simulator, no coexistence, no high-torque, no release-ready, and no wheel-movement claim."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-payload-source-candidates --response-semantic-fixtures ci/hardware/moza-r5/2026-05-13/vendor-status-response-semantic-fixtures.json --protocol-evidence-review ci/hardware/moza-r5/2026-05-13/vendor-protocol-evidence-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-payload-source-candidates.json --overwrite",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_payload_source_candidates -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe_response_semantic_fixtures -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_response_semantic_fixtures -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo clippy --locked -p racing-wheel-hid-moza-protocol --all-targets --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "review-payload-source-semantic-fixtures"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-review-payload-source-semantic-fixtures"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-payload-source-semantic-review.json"
claim_boundary = "No-output fixture-backed review only; consumes the payload-source candidate receipt, adds decoder coverage for four nonzero 0x8E device-to-host setting-change samples, classifies them as status-source questions with insufficient authority-state evidence, and keeps no HID or serial open, no read-only query send, no output/configuration/firmware/PIDFF command, no corrected probe readiness, no authorization planning, no motion attempt, no semantic decode claim, no registry promotion, no tuple sendability, no native-control, no native-visible, no smoke-ready, no simulator, no coexistence, no high-torque, no release-ready, and no wheel-movement claim."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-payload-source-semantic-review --payload-source-candidates ci/hardware/moza-r5/2026-05-13/vendor-status-payload-source-candidates.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-payload-source-semantic-review.json --overwrite",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_payload_source_semantic_review -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_payload_source_candidates -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --test vendor_response_semantic_fixtures -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "stage-0x8e-timing-correlation-plan"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-stage-0x8e-timing-correlation-plan"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-plan.json"
claim_boundary = "No-output timing-correlation capture plan only; consumes the payload-source semantic review, stages a passive Pit House 0x8E event-marker capture path, records no capture or timing proof, opens no HID or serial device, sends no read-only query or output/configuration/firmware/PIDFF command, and keeps live probe, authorization planning, motion attempts, semantic decode, registry promotion, tuple sendability, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, release-ready, and wheel-movement claims false."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-timing-correlation-plan --semantic-review ci/hardware/moza-r5/2026-05-13/vendor-status-payload-source-semantic-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-plan.json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-0x8e-timing-plan.json --md-out ci/hardware/moza-r5/2026-05-13/index.md",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_timing_correlation_plan -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_payload_source_semantic_review -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "review-existing-0x8e-timing-correlation"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-review-existing-0x8e-timing-correlation"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-review.json"
claim_boundary = "No-output timing-correlation review only; consumes the payload-source semantic review plus the accepted Pit House setting-change derived summary and notes, records that the existing 0x8E samples are static/single-sample and lack event-marker timing proof, opens no HID or serial device, sends no read-only query or output/configuration/firmware/PIDFF command, and keeps live probe, authorization planning, motion attempts, semantic decode, registry promotion, tuple sendability, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, release-ready, and wheel-movement claims false."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-timing-correlation-review --semantic-review ci/hardware/moza-r5/2026-05-13/vendor-status-payload-source-semantic-review.json --summary ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/sniff-summary.json --operator-notes ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/operator-notes.md --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-review.json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-0x8e-timing-review.json --md-out ci/hardware/moza-r5/2026-05-13/index.md",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_timing_correlation_review -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_timing_correlation_plan -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "audit-vendor-status-movement-blocker"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-audit-vendor-status-movement-blocker"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-movement-blocker-audit.json"
claim_boundary = "No-output stored-receipt blocker audit only; consumes the original zero-response status probe, demux follow-up, framing diagnoses, endpoint diagnoses, timing-correlation plan, and timing-correlation review to record that broad serial ownership/line-setting/framing/scan-window explanations are not the current blocker, while authority endpoint/command mismatch and missing timing-correlated payload-bearing status-source evidence still block live probes, authorization planning, PIDFF reruns, force escalation, and motion. Opens no HID or serial device, sends no read-only query or output/configuration/firmware/PIDFF command, and keeps native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, release-ready, and wheel-movement claims false."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-movement-blocker-audit --status-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix.json --demux-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix-demux.json --framing-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-framing-diagnosis.json --extended-scan-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-extended-scan-diagnosis.json --authority-endpoint-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-endpoint-diagnosis.json --payload-rerun-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-payload-rerun-diagnosis.json --timing-correlation-plan ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-plan.json --timing-correlation-review ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-movement-blocker-audit.json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-status-blocker-audit.json --md-out ci/hardware/moza-r5/2026-05-13/index.md",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe_movement_blocker_audit -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "route-native-motion-blocker-to-0x8e-timing-capture"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-route-native-motion-blocker-to-0x8e-timing-capture"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-movement-blocker-audit.json"
claim_boundary = "No-output navigation-only update; teaches bench-wizard and artifact-index vendor navigation to route the native-motion blocker to the staged passive Pit House 0x8E event-marker timing-correlation capture and no-output review. It opens no HID or serial device, sends no read-only query or output/configuration/firmware/PIDFF command, creates no authorization, runs no capture, and keeps live probe, authorization planning, PIDFF rerun, force escalation, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, release-ready, and wheel-movement claims false."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-native-motion-blocker-navigation.json --md-out ci/hardware/moza-r5/2026-05-13/index.md",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/bench-wizard-native-motion-blocker-navigation.json --md-out target/moza-current/bench-wizard-native-motion-blocker-navigation.md",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "sniff-summary-packet-timestamp-samples"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-sniff-summary-packet-timestamp-samples"
current_pointer = "crates/cli/src/commands/hardware.rs"
claim_boundary = "No-output sniff-summary metadata plumbing only; optional payload samples now preserve packet/frame timestamp metadata for future passive 0x8E timing-correlation review without opening HID or serial, sending read-only queries, running a live capture, sending output/configuration/firmware/PIDFF commands, creating authorization, promoting semantic decode or tuple sendability, or claiming native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, release-ready, or wheel movement."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl hardware_sniff_summary -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_timing_correlation_review -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo clippy --locked -p racing-wheel-hid-moza-protocol --all-targets --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "stage-0x8e-timing-correlation-sniff-handoff"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-stage-0x8e-timing-correlation-sniff-handoff"
current_pointer = "ci/hardware/sniff/moza-r5/2026-05-13/pit-house-0x8e-timing-correlation/sniff-plan.json"
claim_boundary = "No-output passive capture handoff hardening only; adds a checked-in pit-house-0x8e-timing-correlation sniff plan, operator-note event marker requirements, schema and bundle validation, and a 180000 ms hardware-doctor selector-verified capture template without opening HID or serial, sending read-only queries, running a live capture, sending output/configuration/firmware/PIDFF commands, creating authorization, promoting semantic decode or tuple sendability, or claiming native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, release-ready, or wheel movement."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- --json hardware sniff-plan --family moza-r5 --scenario pit-house-0x8e-timing-correlation --lane ci/hardware/moza-r5/2026-05-13 --operator Steven --device-note \"Moza R5 PID 0x0004 with KS/ES wheels, SR-P pedals, and HBP handbrake attached through the R5 hub\" --capture-tool usbpcap --capture-tool wireshark --capture-tool tshark --platform-hint windows --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-0x8e-timing-correlation/sniff-plan.json",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-timing-correlation-plan --semantic-review ci/hardware/moza-r5/2026-05-13/vendor-status-payload-source-semantic-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-plan.json --overwrite",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl hardware_sniff -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_bundle -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_timing_correlation_plan -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "pin-status-probe-rerun-gate"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-pin-status-probe-rerun-gate"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-movement-blocker-audit.json"
claim_boundary = "No-output stored-receipt gate hardening only; pins that a clean observe-only hardware doctor proves bench visibility but is not permission to rerun the read-only status probe or attempt motion while the timing-correlated 0x8E authority/status source is missing. Opens no HID or serial device, sends no read-only query or output/configuration/firmware/PIDFF command, creates no authorization, runs no capture, and keeps live probe, PIDFF rerun, force escalation, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, release-ready, and wheel-movement claims false."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-movement-blocker-audit --status-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix.json --demux-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix-demux.json --framing-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-framing-diagnosis.json --extended-scan-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-extended-scan-diagnosis.json --authority-endpoint-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-endpoint-diagnosis.json --payload-rerun-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-payload-rerun-diagnosis.json --timing-correlation-plan ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-plan.json --timing-correlation-review ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-movement-blocker-audit.json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-status-rerun-gate.json --md-out ci/hardware/moza-r5/2026-05-13/index.md",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe_movement_blocker_audit -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "correlate-0x8e-timing-review-events"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-correlate-0x8e-timing-review-events"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-review.json"
claim_boundary = "No-output timing-review logic only; parses operator event-marker timestamps and packet timestamp samples to classify whether future 0x8E samples vary near named Pit House events. Opens no HID or serial device, sends no read-only query or output/configuration/firmware/PIDFF command, creates no authorization, runs no capture, promotes no semantic decode or tuple sendability, and keeps live probe, PIDFF rerun, force escalation, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, release-ready, and wheel-movement claims false."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-timing-correlation-review --semantic-review ci/hardware/moza-r5/2026-05-13/vendor-status-payload-source-semantic-review.json --summary ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/sniff-summary.json --operator-notes ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/operator-notes.md --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-review.json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-movement-blocker-audit --status-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix.json --demux-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix-demux.json --framing-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-framing-diagnosis.json --extended-scan-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-extended-scan-diagnosis.json --authority-endpoint-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-endpoint-diagnosis.json --payload-rerun-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-payload-rerun-diagnosis.json --timing-correlation-plan ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-plan.json --timing-correlation-review ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-movement-blocker-audit.json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-0x8e-event-review.json --md-out ci/hardware/moza-r5/2026-05-13/index.md",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_timing_correlation_review -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "reprocess-setting-change-0x8e-timestamps"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-reprocess-setting-change-0x8e-timestamps"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-review.json"
claim_boundary = "No-output derived-evidence reprocessing only; reprocesses the existing local Pit House setting-change pcap into checked-in derived summary and manifest artifacts, teaches timing review to parse RFC3339 packet timestamps, records same-tuple 0x8E payload variation, and keeps missing event-marker timing proof as the active blocker without opening HID or serial, sending read-only queries, running a live capture, sending output/configuration/firmware/PIDFF commands, creating authorization, promoting semantic decode or tuple sendability, or claiming native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, release-ready, or wheel movement."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-summary --pcapng H:/Code/Rust/OpenRacing-swarm/target/sniff/pit-house-setting-change-repeat-02/capture.pcapng --vendor 0x346E --product 0x0004 --include-payload-samples --max-samples-per-report 32 --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/sniff-summary.json",
  "cargo run --locked -p wheelctl --bin wheelctl -- hardware sniff-bundle --plan ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/sniff-plan.json --receipt ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/sniff-receipt.json --summary ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/sniff-summary.json --operator-notes ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/operator-notes.md --out target/sniff/pit-house-setting-change-reprocessed/openracing-sniff-bundle.zip --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/sniff-bundle-manifest.json",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-timing-correlation-plan --semantic-review ci/hardware/moza-r5/2026-05-13/vendor-status-payload-source-semantic-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-plan.json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-timing-correlation-review --semantic-review ci/hardware/moza-r5/2026-05-13/vendor-status-payload-source-semantic-review.json --summary ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/sniff-summary.json --operator-notes ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/operator-notes.md --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-review.json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-movement-blocker-audit --status-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix.json --demux-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix-demux.json --framing-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-framing-diagnosis.json --extended-scan-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-extended-scan-diagnosis.json --authority-endpoint-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-endpoint-diagnosis.json --payload-rerun-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-payload-rerun-diagnosis.json --timing-correlation-plan ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-plan.json --timing-correlation-review ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-movement-blocker-audit.json --overwrite",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_timing_correlation_review -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "materialize-0x8e-capture-selector-handoff"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-materialize-0x8e-capture-selector-handoff"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-plan.json"
claim_boundary = "No-output handoff metadata hardening only; marks the 0x8E timing-correlation sniff-capture template as a placeholder that must not be run until sniff-notes-template renders a concrete current hardware-doctor selector-bound command in operator-notes.md. Opens no HID or serial device, sends no read-only query or output/configuration/firmware/PIDFF command, runs no live capture, creates no authorization, promotes no semantic decode or tuple sendability, and keeps live probe, PIDFF rerun, force escalation, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, release-ready, and wheel-movement claims false."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-timing-correlation-plan --semantic-review ci/hardware/moza-r5/2026-05-13/vendor-status-payload-source-semantic-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-plan.json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-movement-blocker-audit --status-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix.json --demux-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix-demux.json --framing-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-framing-diagnosis.json --extended-scan-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-extended-scan-diagnosis.json --authority-endpoint-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-endpoint-diagnosis.json --payload-rerun-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-payload-rerun-diagnosis.json --timing-correlation-plan ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-plan.json --timing-correlation-review ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-movement-blocker-audit.json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-0x8e-selector-handoff.json --md-out ci/hardware/moza-r5/2026-05-13/index.md",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/bench-wizard-0x8e-selector-handoff.json --md-out target/moza-current/bench-wizard-0x8e-selector-handoff.md",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_timing_correlation_plan -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard_routes_native_motion_blocker_to_0x8e_timing_capture -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "refresh-read-only-status-demux-rerun-blocker"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-refresh-read-only-status-demux-rerun-blocker"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix-demux.json"
claim_boundary = "Read-only hardware status rerun evidence only; after a fresh observe-only hardware doctor, the COM4 probe sent registry-approved read-only queries with a 64-frame scan cap, decoded seven non-authority status replies, failed the two authority/status replies closed, and kept mode/safety unknown. Opens no HID output path, sends no PIDFF/output/configuration/firmware/DFU/high-torque command, creates no authorization, promotes no tuple sendability, runs no motion, and keeps wheel_moved_under_openracing=false, visible_motion_verified=false, output_was_sent=false, and authority_state=blocked."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- --json hardware doctor --json-out target/moza-current/vendor-status-mode-matrix-demux-hardware-doctor.json",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-probe --serial-port COM4 --baud-rate 115200 --timeout-ms 250 --max-response-frames-per-query 64 --confirm-read-only-query --json-out target/moza-current/vendor-status-mode-matrix-rerun.json",
  "copy target/moza-current/vendor-status-mode-matrix-rerun.json ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix-demux.json",
  "copy target/moza-current/vendor-status-mode-matrix-demux-hardware-doctor.json ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix-demux-hardware-doctor.json",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-movement-blocker-audit --status-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix.json --demux-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix-demux.json --framing-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-framing-diagnosis.json --extended-scan-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-extended-scan-diagnosis.json --authority-endpoint-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-endpoint-diagnosis.json --payload-rerun-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-payload-rerun-diagnosis.json --timing-correlation-plan ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-plan.json --timing-correlation-review ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-movement-blocker-audit.json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/artifact-index-read-only-rerun-blocker.json --md-out ci/hardware/moza-r5/2026-05-13/index.md",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/bench-wizard-read-only-rerun-blocker.json --md-out target/moza-current/bench-wizard-read-only-rerun-blocker.md",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "gate-0x8e-placeholder-capture-command"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-gate-0x8e-placeholder-capture-command"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-movement-blocker-audit.json"
claim_boundary = "No-output bench-wizard navigation hardening only; the 0x8E timing-correlation sniff-capture placeholder remains visible as an external capture template for review, but it is no longer rendered as a runnable Next Operator Command until sniff-notes-template materializes a concrete current hardware-doctor selector-bound command. Opens no HID or serial device, sends no read-only query/output/configuration/firmware/PIDFF command, runs no live capture, creates no authorization, promotes no semantic decode or tuple sendability, and keeps live probe, PIDFF rerun, force escalation, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, release-ready, and wheel-movement claims false."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-current/bench-wizard-0x8e-placeholder-gate.json --md-out target/moza-current/bench-wizard-0x8e-placeholder-gate.md",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard_routes_native_motion_blocker_to_0x8e_timing_capture -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "gate-0x8e-event-marker-timestamps"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-gate-0x8e-event-marker-timestamps"
current_pointer = "crates/cli/src/commands/hardware.rs"
claim_boundary = "No-output passive evidence validation only; pit-house-0x8e-timing-correlation operator notes must now carry parseable RFC3339 event-marker timestamps in chronological order before a bundle can be accepted. Opens no HID or serial device, sends no read-only query/output/configuration/firmware/PIDFF command, runs no live capture, creates no authorization, promotes no semantic decode or tuple sendability, and keeps live probe, PIDFF rerun, force escalation, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, release-ready, and wheel-movement claims false."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_bundle -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "gate-0x8e-review-capture-window"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-gate-0x8e-review-capture-window"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-review.json"
claim_boundary = "No-output timing-review hardening only; vendor-status-timing-correlation-review now refuses to surface a candidate timing correlation unless event markers are ordered and target sample timestamps fall inside the declared capture window. Opens no HID or serial device, sends no read-only query/output/configuration/firmware/PIDFF command, runs no live capture, creates no authorization, promotes no semantic decode or tuple sendability, and keeps live probe, PIDFF rerun, force escalation, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, release-ready, and wheel-movement claims false."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-timing-correlation-review --semantic-review ci/hardware/moza-r5/2026-05-13/vendor-status-payload-source-semantic-review.json --summary ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/sniff-summary.json --operator-notes ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/operator-notes.md --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-review.json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-movement-blocker-audit --status-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix.json --demux-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix-demux.json --framing-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-framing-diagnosis.json --extended-scan-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-extended-scan-diagnosis.json --authority-endpoint-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-endpoint-diagnosis.json --payload-rerun-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-payload-rerun-diagnosis.json --timing-correlation-plan ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-plan.json --timing-correlation-review ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-movement-blocker-audit.json --overwrite",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_timing_correlation_review -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "refine-0x8e-timing-blocker-diagnostics"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-refine-0x8e-timing-blocker-diagnostics"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-review.json"
claim_boundary = "No-output diagnostic precision only; vendor-status-timing-correlation-review now distinguishes missing target packet timestamps from missing operator event-marker timing, counts timestamped target 0x8E samples even when the capture window is unavailable, regenerates the timing review and movement blocker audit, and keeps live probe, authorization planning, PIDFF rerun, force escalation, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, release-ready, output, and wheel-movement claims false."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-timing-correlation-review --semantic-review ci/hardware/moza-r5/2026-05-13/vendor-status-payload-source-semantic-review.json --summary ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/sniff-summary.json --operator-notes ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/operator-notes.md --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-review.json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-movement-blocker-audit --status-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix.json --demux-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix-demux.json --framing-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-framing-diagnosis.json --extended-scan-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-extended-scan-diagnosis.json --authority-endpoint-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-endpoint-diagnosis.json --payload-rerun-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-payload-rerun-diagnosis.json --timing-correlation-plan ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-plan.json --timing-correlation-review ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-movement-blocker-audit.json --overwrite",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_timing_correlation_review -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "render-0x8e-event-marker-helper"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-render-0x8e-event-marker-helper"
current_pointer = "crates/cli/src/commands/hardware.rs"
claim_boundary = "No-output operator-note tooling only; sniff-notes-template now renders concrete PowerShell UTC marker commands for the pit-house-0x8e-timing-correlation notes file so the next passive capture can fill all required event markers. Opens no HID or serial device, sends no read-only query or output/configuration/firmware/PIDFF command, runs no live capture, creates no authorization, promotes no semantic decode or tuple sendability, and keeps live probe, PIDFF rerun, force escalation, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, release-ready, output, and wheel-movement claims false."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_notes_template -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl pit_house_0x8e_timing -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "gate-0x8e-semantic-event-marker-correlation"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-gate-0x8e-semantic-event-marker-correlation"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-review.json"
claim_boundary = "No-output timing-review hardening only; 0x8E candidate timing correlation now ignores administrative markers such as capture start/stop and hardware-doctor review, requiring payload variation near semantic operator events before a candidate can surface. Opens no HID or serial device, sends no read-only query/output/configuration/firmware/PIDFF command, runs no live capture, creates no authorization, promotes no semantic decode or tuple sendability, and keeps live probe, PIDFF rerun, force escalation, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, release-ready, output, and wheel-movement claims false."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-timing-correlation-review --semantic-review ci/hardware/moza-r5/2026-05-13/vendor-status-payload-source-semantic-review.json --summary ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/sniff-summary.json --operator-notes ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/operator-notes.md --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-review.json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-movement-blocker-audit --status-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix.json --demux-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix-demux.json --framing-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-framing-diagnosis.json --extended-scan-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-extended-scan-diagnosis.json --authority-endpoint-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-endpoint-diagnosis.json --payload-rerun-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-payload-rerun-diagnosis.json --timing-correlation-plan ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-plan.json --timing-correlation-review ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-movement-blocker-audit.json --overwrite",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_timing_correlation_review -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "surface-0x8e-postcapture-receipts"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-surface-0x8e-postcapture-receipts"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-plan.json"
claim_boundary = "No-output handoff completion only; the 0x8E timing-correlation plan and movement-blocker audit now surface non-claiming sniff-receipt and sniff-bundle commands after the passive capture, without opening HID or serial, sending read-only queries, running a live capture, sending output/configuration/firmware/PIDFF commands, creating authorization, promoting semantic decode or tuple sendability, or claiming native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, release-ready, output, or wheel movement."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-timing-correlation-plan --semantic-review ci/hardware/moza-r5/2026-05-13/vendor-status-payload-source-semantic-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-plan.json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-movement-blocker-audit --status-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix.json --demux-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix-demux.json --framing-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-framing-diagnosis.json --extended-scan-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-extended-scan-diagnosis.json --authority-endpoint-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-endpoint-diagnosis.json --payload-rerun-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-payload-rerun-diagnosis.json --timing-correlation-plan ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-plan.json --timing-correlation-review ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-movement-blocker-audit.json --overwrite",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard_routes_native_motion_blocker_to_0x8e_timing_capture -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_timing_correlation_plan -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "add-0x8e-sniff-marker-command"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-add-0x8e-sniff-marker-command"
current_pointer = "crates/cli/src/commands/hardware.rs"
claim_boundary = "No-output operator-note tooling only; wheelctl hardware sniff-marker stamps the pit-house-0x8e-timing-correlation operator notes and writes a non-claiming local marker receipt without opening HID or serial, sending read-only queries, running a live capture, sending output/configuration/firmware/PIDFF commands, creating authorization, promoting semantic decode or tuple sendability, or claiming native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, release-ready, output, or wheel movement."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-timing-correlation-plan --semantic-review ci/hardware/moza-r5/2026-05-13/vendor-status-payload-source-semantic-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-plan.json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-movement-blocker-audit --status-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix.json --demux-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix-demux.json --framing-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-framing-diagnosis.json --extended-scan-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-extended-scan-diagnosis.json --authority-endpoint-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-endpoint-diagnosis.json --payload-rerun-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-payload-rerun-diagnosis.json --timing-correlation-plan ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-plan.json --timing-correlation-review ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-movement-blocker-audit.json --overwrite",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_marker -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_timing_correlation_plan -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard_routes_native_motion_blocker_to_0x8e_timing_capture -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "align-0x8e-marker-order"
status = "completed"
spec = "docs/specs/OR-SPEC-0002-moza-r5-vendor-authority-test-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-align-0x8e-marker-order"
current_pointer = "ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-plan.json"
claim_boundary = "No-output operator-handoff alignment only; the 0x8E timing-correlation required marker lists now present hardware_doctor_selector_reviewed_utc before capture_start_utc to match existing validators. Opens no HID or serial device, sends no read-only query or output/configuration/firmware/PIDFF command, runs no live capture, creates no authorization, promotes no semantic decode or tuple sendability, and keeps live probe, PIDFF rerun, force escalation, native-control, native-visible, smoke-ready, simulator, coexistence, high-torque, release-ready, output, and wheel-movement claims false."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- --json hardware sniff-plan --family moza-r5 --scenario pit-house-0x8e-timing-correlation --lane ci/hardware/moza-r5/2026-05-13 --operator Steven --device-note \"Moza R5 PID 0x0004 with KS/ES wheels, SR-P pedals, and HBP handbrake attached through the R5 hub\" --capture-tool usbpcap --capture-tool wireshark --capture-tool tshark --platform-hint windows --json-out ci/hardware/sniff/moza-r5/2026-05-13/pit-house-0x8e-timing-correlation/sniff-plan.json",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-timing-correlation-plan --semantic-review ci/hardware/moza-r5/2026-05-13/vendor-status-payload-source-semantic-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-plan.json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-timing-correlation-review --semantic-review ci/hardware/moza-r5/2026-05-13/vendor-status-payload-source-semantic-review.json --summary ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/sniff-summary.json --operator-notes ci/hardware/sniff/moza-r5/2026-05-13/pit-house-setting-change/operator-notes.md --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-review.json --overwrite",
  "cargo run --locked -p wheelctl --bin wheelctl -- --json moza vendor-status-movement-blocker-audit --status-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix.json --demux-probe ci/hardware/moza-r5/2026-05-13/vendor-status-mode-matrix-demux.json --framing-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-framing-diagnosis.json --extended-scan-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-extended-scan-diagnosis.json --authority-endpoint-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-endpoint-diagnosis.json --payload-rerun-diagnosis ci/hardware/moza-r5/2026-05-13/vendor-status-authority-payload-rerun-diagnosis.json --timing-correlation-plan ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-plan.json --timing-correlation-review ci/hardware/moza-r5/2026-05-13/vendor-status-timing-correlation-review.json --json-out ci/hardware/moza-r5/2026-05-13/vendor-status-movement-blocker-audit.json --overwrite",
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_notes_template -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl sniff_bundle -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_timing_correlation_plan -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_timing_correlation_review -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard_routes_native_motion_blocker_to_0x8e_timing_capture -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_status_probe -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl vendor_fake_transport -- --nocapture",
  "cargo test --locked -p racing-wheel-hid-moza-protocol --all-features -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl --all-features -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "native-visible-promotion"
status = "blocked"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-native-visible-promotion"
current_pointer = "ci/hardware/moza-r5/2026-05-13/native-controlled-angle-closed-loop-smoke.json"
claim_boundary = "May promote native_visible_ready only if the verifier accepts real visible motion; no smoke-ready or release-ready claim."
blocked_by = "A passing native-visible hardware receipt."
commands = [
  "wheelctl moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out ci/hardware/moza-r5/2026-05-13/native-visible-verification.json --json",
  "wheelctl moza promote-manifest --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out ci/hardware/moza-r5/2026-05-13/manifest-promotion-native-visible.json --json",
  "wheelctl moza audit-lane --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out ci/hardware/moza-r5/2026-05-13/lane-audit-native-visible.json --json",
]
