id = "moza-native-visible-lane"
title = "Moza R5 native visible motion lane"
status = "active"
owner = "hardware"
created = "2026-05-18"

proposal = "docs/proposals/OR-PROP-0001-moza-native-visible-lane.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md"

specs = [
  "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md",
]

adrs = [
  "docs/adr/0009-hardware-validation-evidence-state-machine.md",
]

objective = """
Move the Moza R5 lane from native_response_ready to native_visible_ready using receipt-backed, exact-command hardware attempts while keeping PIDFF diagnosis, passive sniffing, Pit House, SimHub, simulator telemetry, and simulator FFB claim boundaries explicit.
"""

end_state = [
  "A same-lane native visible-motion receipt passes the native_actuator_visible_smoke gate.",
  "The lane manifest, verifier receipt, and audit record native_visible_ready without smoke-ready, release-ready, high-torque, simulator, or vendor-app claims.",
  "Further controlled movement, external compatibility, simulator telemetry, and bounded simulator FFB remain separately gated work.",
]

claim_boundaries = [
  "This active goal does not authorize hardware output.",
  "No direct report 0x20, high torque, serial config, firmware, or DFU is in scope.",
  "Pit House, SimHub, simulator evidence, and passive USB sniff artifacts are not native-control prerequisites.",
  "Dry-runs, lifecycle traces, plans, indexes, and bench-wizard receipts do not satisfy native-visible readiness.",
]

status_docs = [
  "docs/NOW_NEXT_LATER.md",
  "ci/hardware/moza-r5/2026-05-13/index.md",
  "docs/hardware/moza-r5-validation.md",
  "docs/hardware/moza-r5-artifact-checklist.md",
  "docs/hardware/moza-r5-completion-audit.md",
  "docs/hardware/moza-validation-matrix.md",
  "docs/hardware/moza-r5-controlled-angle-analysis.md",
  "docs/hardware/moza-r5-pidff-lifecycle-trace.md",
  "docs/hardware/moza-r5-pidff-effect-lifecycle-plan.md",
]

[[work_item]]
id = "activate-source-of-truth"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-activate-source-of-truth"
current_pointer = "ci/hardware/moza-r5/2026-05-13/index.md"
claim_boundary = "Docs and source-of-truth metadata only; no hardware output, no authorization receipt, and no readiness promotion."
commands = [
  "python scripts/policy_file.py",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "git diff --check",
]

[[work_item]]
id = "record-completion-audit"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-record-completion-audit"
current_pointer = "docs/hardware/moza-r5-completion-audit.md"
claim_boundary = "Docs-only audit; no hardware output, no authorization receipt, no readiness promotion, and no new hardware artifact claim."
commands = [
  "python scripts/policy_file.py",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza bench-wizard --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-bench-wizard-current.json --md-out target/moza-bench-wizard-current.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-native-visible-current.json --json",
  "git diff --check",
]

[[work_item]]
id = "document-input-role-candidates"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-document-input-role-candidates"
current_pointer = "ci/hardware/moza-r5/2026-05-13/pre-output-readiness.json"
claim_boundary = "Candidate-only input slot navigation; no hardware output, no authorization receipt, no readiness promotion, and no role-specific semantic claim."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl input_role -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl moza -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "harden-artifact-claim-boundaries"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-harden-artifact-claim-boundaries"
current_pointer = "crates/cli/src/commands/moza.rs"
claim_boundary = "Regression coverage only; valid failed verifier receipts remain diagnostic artifacts and do not become native-visible, smoke-ready, authorization, or output claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl artifact_index -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl moza -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "git diff --check",
]

[[work_item]]
id = "surface-attempt-03-clearance-phrase"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-surface-attempt-03-clearance-phrase"
current_pointer = "crates/cli/src/commands/moza.rs"
claim_boundary = "Operator-packet UX only; bench-wizard remains read-only and does not create authorization receipts, output commands, or readiness claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl moza -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "git diff --check",
]

[[work_item]]
id = "harden-attempt-03-authorization-handoff"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-harden-attempt-03-authorization-handoff"
current_pointer = "crates/cli/src/commands/moza.rs"
claim_boundary = "Bench-wizard navigation only; a recorded attempt-03 authorization is surfaced as a separate handoff state without emitting output commands, creating authorization receipts, or claiming native-visible readiness."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl controlled_angle -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl moza -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "git diff --check",
]

[[work_item]]
id = "surface-attempt-03-planned-artifacts"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-surface-attempt-03-planned-artifacts"
current_pointer = "ci/hardware/moza-r5/2026-05-13/index.md"
claim_boundary = "Artifact-index navigation only; missing attempt-03 authorization/output receipts are surfaced as planned_missing without creating authorization receipts, output receipts, or native-visible claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl artifact_index -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl moza -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "refresh-input-semantic-candidate-artifacts"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-refresh-input-semantic-candidate-artifacts"
current_pointer = "ci/hardware/moza-r5/2026-05-13/lane-capture-analysis.json"
claim_boundary = "No-output artifact refresh only; candidate R5 V1 extended slots are recorded as diagnostic input-topology evidence without creating role-specific semantic claims, authorization receipts, output receipts, or native-visible claims."
commands = [
  "cargo run --locked -p wheelctl --bin wheelctl -- moza analyze-lane --lane ci/hardware/moza-r5/2026-05-13 --json-out ci/hardware/moza-r5/2026-05-13/lane-capture-analysis.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza sync-role-status --lane ci/hardware/moza-r5/2026-05-13 --json-out ci/hardware/moza-r5/2026-05-13/role-status-sync.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza artifact-index --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-artifact-index-input-semantics.json --md-out ci/hardware/moza-r5/2026-05-13/index.md --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza sync-role-status --lane ci/hardware/moza-r5/2026-05-13 --check --json-out target/moza-role-status-check.json --json",
  "cargo run --locked -p wheelctl --bin wheelctl -- moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage passive --json-out target/moza-passive-after-input-semantics-refresh.json --json",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "python scripts/policy_file.py",
  "git diff --check",
]

[[work_item]]
id = "surface-pit-house-compatibility-progress"
status = "completed"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-surface-pit-house-compatibility-progress"
current_pointer = "crates/cli/src/commands/moza.rs"
claim_boundary = "No-output navigation only; Pit House availability and partial case progress are external-smoke diagnostics and do not become native-control, native-visible, smoke-ready, authorization, or output claims."
commands = [
  "python scripts/cargo_fmt_workspace.py",
  "cargo test --locked -p wheelctl --bin wheelctl artifact_navigation_surfaces_pit_house -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl artifact_index -- --nocapture",
  "cargo test --locked -p wheelctl --bin wheelctl bench_wizard -- --nocapture",
  "cargo clippy --locked -p wheelctl --bin wheelctl -- -D warnings",
  "cargo run --locked -p openracing-tools --bin package-surface -- --check",
  "git diff --check",
]

[[work_item]]
id = "attempt-03-authorization"
status = "blocked"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-attempt-03-authorization"
current_pointer = "ci/hardware/moza-r5/2026-05-13/native-controlled-angle-attempt-03-preflight.json"
claim_boundary = "May create one exact authorization receipt only after fresh command-bound bench-clear for attempt 03; still no motion claim."
blocked_by = "Fresh command-bound bench-clear from the operator for attempt 03."
required_bench_clear_evidence = "bench clear for exactly one Moza controlled-angle attempt 03: target 1 degree, max 5%, timeout 2000 ms, strategy pidff-bounded-effect, profile bounded-pidff-effect-lifecycle-v1, R5 stable, KS attached securely, hands clear, wheel clear, prior undertravel receipts preserved"
commands = [
  "wheelctl moza pre-output-readiness --lane ci/hardware/moza-r5/2026-05-13 --json-out target/moza-pre-output-before-attempt-03.json --json",
  "wheelctl moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out target/moza-native-visible-before-attempt-03.json --json",
]

[[work_item]]
id = "attempt-03-output"
status = "blocked"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-attempt-03-output"
current_pointer = "ci/hardware/moza-r5/2026-05-13/native-controlled-angle-attempt-03-authorization.json"
claim_boundary = "May run exactly one authorized 1 degree, 5 percent, 2000 ms controlled-angle attempt using bounded-pidff-effect-lifecycle-v1; no rerun or escalation."
blocked_by = "A matching attempt-03 authorization receipt."
commands = [
  "wheelctl moza controlled-angle-smoke --device hid-0x346E-0x0004-if2-0x0001-0x0004 --lane ci/hardware/moza-r5/2026-05-13 --prior-actuator-proof ci/hardware/moza-r5/2026-05-13/native-actuator-profile-smoke.json --steering-proof ci/hardware/moza-r5/2026-05-13/steering-angle-stream-proof.json --authorization-proof ci/hardware/moza-r5/2026-05-13/native-controlled-angle-attempt-03-authorization.json --target-degrees 1 --profile bounded-pidff-effect-lifecycle-v1 --max-percent 5 --timeout-ms 2000 --strategy pidff-bounded-effect --confirm-controlled-angle --json-out ci/hardware/moza-r5/2026-05-13/native-controlled-angle-attempt-03-smoke.json --json",
]

[[work_item]]
id = "native-visible-promotion"
status = "blocked"
spec = "docs/specs/OR-SPEC-0001-moza-native-visible-lane.md"
adr = "docs/adr/0009-hardware-validation-evidence-state-machine.md"
plan = "plans/moza-native-visible-lane/implementation-plan.md#work-item-native-visible-promotion"
current_pointer = "ci/hardware/moza-r5/2026-05-13/native-controlled-angle-attempt-03-smoke.json"
claim_boundary = "May promote native_visible_ready only if the verifier accepts real visible motion; no smoke-ready or release-ready claim."
blocked_by = "A passing native-visible hardware receipt."
commands = [
  "wheelctl moza verify-bundle --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out ci/hardware/moza-r5/2026-05-13/native-visible-verification.json --json",
  "wheelctl moza promote-manifest --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out ci/hardware/moza-r5/2026-05-13/manifest-promotion-native-visible.json --json",
  "wheelctl moza audit-lane --lane ci/hardware/moza-r5/2026-05-13 --stage native-visible-ready --json-out ci/hardware/moza-r5/2026-05-13/lane-audit-native-visible.json --json",
]
