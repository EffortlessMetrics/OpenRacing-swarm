[package]
name = "openracing-pidff-common"
version = "0.1.0"
edition = "2024"
description = "Shared USB HID PID (PIDFF) effect encoders for force feedback devices"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
workspace-hack = { version = "0.1", path = "../../workspace-hack" }

[dev-dependencies]
proptest = { workspace = true }

[lints]
workspace = true
