[package]
name = "openracing-telemetry-adapters"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage = "https://github.com/EffortlessMetrics/OpenRacing"
documentation = "https://docs.rs/openracing-telemetry-adapters"
publish = true
description = "Game-specific telemetry adapters extracted into a standalone crate"
readme = "README.md"
keywords = ["telemetry", "gaming", "adapters", "ffb", "racing"]


[package.metadata.cargo-udeps.ignore]
normal = ["workspace-hack"]

categories = ["game-development"]
[dependencies]
anyhow = { workspace = true }
async-trait = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
serde_yaml = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
quick-xml = { workspace = true }
openracing-telemetry = { path = "../telemetry-core", version = "0.1.0" }
workspace-hack = { version = "0.1", path = "../../workspace-hack" }

[dev-dependencies]
proptest = { workspace = true }
tempfile = "3.25.0"
insta = { version = "1.46.3", features = ["yaml", "filters"] }

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3.9", features = [
    "handleapi",
    "memoryapi",
    "winnt",
    "synchapi",
    "tlhelp32",
] }

[lints]
workspace = true
